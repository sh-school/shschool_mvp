# CLAUDE.md — تعليمات العمل على منصة SchoolOS
> **ملاحظة:** هذا الملف يجب أن يبقى في جذر المشروع حتى يقرأه Claude Code تلقائياً.
> النسخة الكاملة + التاريخية محفوظة في: `AAdocs/claude/claude-config/CLAUDE.md`

## !!! القاعدة رقم 1 — الأهم على الإطلاق !!!

> **كلُّ جلسةٍ تعمل في شجرة عملها هي، وتراها حيّةً على منفذها هي.**
>
> كانت القاعدةُ قبل 2026-09-08: «عدّل في `D:\shschool_mvp` وحدَه، ولا تستعمل
> worktree أبداً». وسببُها أنّ `docker-compose.yml` يربط `- .:/app`، فما كُتب
> في شجرةِ عملٍ لم يكن يُرى على الشاشة. لكنّها عالجت العَرَض بمنع العزل:
> رأسٌ واحدٌ وفهرسٌ واحدٌ وشجرةُ عملٍ واحدةٌ لمحادثاتٍ متوازية — فتبديلُ فرعٍ
> في إحداها يسحب الأرضَ من تحت الأخرى، و`git add -A` في إحداها يبتلع عملَ
> الأخرى نصفَ المكتوب.
>
> والغرضُ من التوازي تسريعُ العمل على المنصّة، فلا يُشترى بتصادمٍ يُبطئه.
> و`docker-compose.session.yml` أزال السبب: خادمٌ لكلّ شجرةٍ على منفذها،
> يشارك قاعدةَ البيانات وredis مع الحزمة الأصليّة.
>
> **فاعمل حيث أنت:** الجلسةُ في شجرةِ عملٍ تحرّر ملفّاتِ شجرتها، والجلسةُ في
> `D:\shschool_mvp` تحرّر ملفّاته. ولا تعبر جلسةٌ إلى مسار جلسةٍ أخرى.

### خادمُ الجلسة

مرّتان لكلّ شجرةِ عمل — `.env` متجاهَلٌ في غيت فلا يصل الشجرةَ من نفسه،
والقاعدةُ تُنسَخ لك وحدَك:

```bash
cp D:/shschool_mvp/.env .env
```

```bash
bash scripts/session-db.sh
```

ثمّ في كلّ مرّة، بمنفذٍ لم تأخذه جلسةٌ أخرى (8001، 8002، 8003…):

```bash
SESSION_DB=$(bash scripts/session-db.sh --name) WEB_PORT=8001 docker compose -p schoolos-$(basename $PWD) --project-directory . -f D:/shschool_mvp/docker-compose.session.yml up -d
```

و`--project-directory .` هو المفتاح: ملفُّ الإنشاء يبقى في الجذر واحداً،
والمساراتُ فيه تُحلّ على شجرتك أنت — فلا يُنسخ إلى كلّ شجرةٍ ولا يُخلّف ملفّاً
غيرَ متتبَّعٍ يمنع سحبَ الفرع لاحقاً.

### ولِمَ قاعدةٌ لكلّ شجرة

كانت الأشجارُ كلُّها على `shschool_db`. فلمّا حذف فرعٌ عموداً بهجرةٍ يومَ
2026-09-11 سقطت ثمانُ صفحاتٍ في **كلّ** شجرةٍ أخرى ساعاتٍ بـ
`column operations_subject.spread_days_scope does not exist` — وشيفرةُ تلك
الأشجار سليمةٌ لا علّةَ فيها. والخادمُ كان معزولاً والقاعدةُ وحدَها مشتركة.

فصار لكلّ شجرةٍ قاعدتُها، تُنسخ من المشتركة أوّلَ مرّة — ثمانيةٌ وستّون
ميغابايتاً في اثنتَي عشرةَ ثانية — **والهجرةُ بعدها تُطبَّق في شجرتك أنت**،
فلا تُصيب غيرَك.

ويبقى شرطٌ واحد: أن تعمل الحزمةُ الأصليّة في `D:\shschool_mvp` — منها
خادمُ القواعد وredis والصورةُ المبنيّة.

## غيت في المحادثات المتوازية

المستودعُ واحد، ورأسُه واحد، ومكدّسُ خبيئته يشترك فيه هو وكلُّ شجراته. فهذه
القيودُ ليست ذوقاً:

1. **لا تبدّل فرعَ شجرةٍ لستَ فيها**، ولا فرعَ `D:\shschool_mvp` من جلسةٍ تعمل
   في شجرة. ولو أردتَ اسماً جديداً للعمل فادفعه بلا تحريك الرأس:
   ```bash
   git push origin HEAD:refs/heads/claude/<اسم-المهمّة>
   ```
2. **لا `git add -A` ولا `git commit -a`** — بمساراتٍ صريحةٍ وحدها.
3. **لا `git stash`** — المكدّسُ مشترك، تخبّئ أنت ويستخرج غيرُك.
4. **لا `git pull --rebase` على شجرةٍ متّسخة** — `git fetch` والدمجُ من GitHub.
5. **`git status` قبل كلّ إيداع.** فإن ظهر ملفٌّ لا تعرفه فقِف واسأل — هو عملُ
   جلسةٍ أخرى لم يُودَع بعد.

## سير العمل

1. عدّل في شجرةِ عملك.
2. `python manage.py collectstatic --noinput` عند تعديل CSS/JS.
3. `git add <مسارات صريحة>` ثمّ `git commit`.
4. `git push origin HEAD:refs/heads/claude/<اسم-المهمّة>` وافتح طلبَ دمج.
5. **لا دفعَ إلى `main` مباشرةً** — البوّاباتُ تمرّ على طلب الدمج.

## كسرُ ذاكرة المتصفّح — لا تفعل شيئاً

كان الطقسُ رفعَ `?v=N` يدوياً في `base.html` و`login.html` عند كلّ تعديل.
وقد أُزيل: أربعةَ عشرَ موضعاً في ستّة قوالب.

فالإنتاجُ يبصم الاسمَ بمحتواه أصلاً عبر
`whitenoise.storage.CompressedManifestStaticFilesStorage` —
`/static/css/custom.06dcd9ebed41.css` — والبصمةُ تتغيّر متى تغيّر الملفّ،
وهو بعينه ما كان `?v=` يحاوله. والتطويرُ يُعيد التحقّق بـ`Last-Modified`.

وكان الرقمُ اليدويُّ يكلّف ثلاثاً: يُنسى فيرى المستخدمُ نمطاً قديماً،
ويتصادم في كلّ طلبِ دمجٍ لأنّه سطرٌ واحدٌ يلمسه كلُّ فرع، ويُشغل المراجعةَ
بما لا معنى له.
