{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "bash scripts/railway-release.sh && echo \"🎯 Starting gunicorn on PORT=${PORT:-8080}\" && exec gunicorn shschool.wsgi:application --bind 0.0.0.0:${PORT:-8080} --workers 3 --timeout 120 --access-logfile - --error-logfile - --log-level info --capture-output",
    "healthcheckPath": "/health/",
    "healthcheckTimeout": 100,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
