"""إخفاءُ الرقم الشخصيّ في القوالب — مُرشِّحٌ واحدٌ لا تكرارَ منطقٍ في اثنين وثلاثين.

    {% load privacy %}
    {{ student.national_id|mask_id }}   →  *******0538

والرقمُ معرّفٌ حكوميٌّ دائمٌ لا يُبدَّل، وكشفُه بالجملة أثقلُ من كشفه واحداً:
شاشةٌ فيها خمسون رقماً تُصوَّر وتُرسَل. وحاجةُ من يقرأ كشفاً أن **يميّز** لا أن
**يعرف** — وأربعُ خاناتٍ أخيرةٌ تكفي للتمييز ولا تُعيد بناءَ الرقم.

ولا يُستعمل في الوثائق الرسميّة (الشهادات وكشوف النتائج والتعهّدات): وثيقةٌ
برقمٍ مستورٍ لا تُغني عن صاحبها. ولا في حقول الإدخال: قيمةٌ مستورةٌ في حقلٍ
تُحفَظ نجوماً.
"""

from django import template

from core.privacy import mask_national_id

register = template.Library()


@register.filter(name="mask_id")
def mask_id(value):
    """آخرُ أربعِ خاناتٍ وما قبلها نجوم — والفارغُ يبقى فارغاً."""
    return mask_national_id(value)
