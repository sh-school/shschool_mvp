"""آليّةُ التصدير المركزيّة (VI-30ب، 2026-09-26) — كلُّ PDF/Excel يمرّ بمدخلٍ واحد.

* `registry` — سجلُّ الأنواع: `kind` ← بنّاءٌ نقيٌّ (`build(school, user, params) -> ExportResult`) تُسجّله التطبيقاتُ
  من `AppConfig.ready()`؛ فلا استيرادَ نازلاً من core إلى التطبيقات.
* `services.respond_export(request, kind)` — المدخل: فحصُ القدرة، ثمّ متزامنٌ (`direct`) أو صفُّ `ExportJob` ومهمّةٌ خلفيّة.
* `runner.run_job(job_id)` — ما يجري في العامل: قدرةٌ، بناءٌ، سقفُ حجمٍ، ورموزُ خطأٍ ثابتة (لا نصَّ استثناء أبداً).
* `views` — الحالةُ (JSON) والتنزيلُ وصفحةُ المتابعة (لمن بلا JS).

لماذا: WeasyPrint وopenpyxl ثقيلان (القياسُ 2026-09-26: 22 من 35 تصديراً فوق 300ms، وأبطؤها 19.9ث متزامنٌ في طلبٍ
يزاحم daphne الذي يخدم WebSocket). راجع `docs/adr` والمذكّرة في الخارطة (VI-30).
"""
