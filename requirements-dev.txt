# Development-only dependencies — not needed in production
# Install: pip install -r requirements-dev.txt

-r requirements.txt

# Testing
pytest==9.1.1
pytest-cov==7.1.0
pytest-django==4.14.0
pytest-asyncio==1.4.0
pytest-xdist==3.8.0          # Parallel test execution across CPU cores
pytest-testmon==2.2.0        # Incremental testing — only run tests affected by changes
coverage==7.16.1
factory-boy==3.3.3
Faker==40.39.0

# Code quality
black==26.5.1
isort==9.0.1
ruff==0.4.4  # ثابتٌ عمداً — يطابق CI (quality-gate.yml) وpre-commit، انظر feedback_ruff_pinned_version

# Type checking — إصداراتُه ثابتةٌ في ملفٍّ واحدٍ يقرؤه CI أيضاً (OWN-30، انظر رأس الملفّ)
-r requirements-mypy.txt

# Load testing
locust==2.46.6

# Mutation testing (quality.yml الأسبوعيّ + make test-mutmut)
mutmut==3.8.0

# تنظيف تاريخ git (أداةُ مالكٍ، لا تدخل صورةَ الإنتاج)
git-filter-repo==2.47.0

# Security scanning
bandit[toml]==1.9.4
pip-audit==2.10.1
detect-secrets==1.5.0

# مدقّق الهجرات — يُسقط أيّ هجرةٍ جديدة تحذف عموداً/جدولاً أو تُسمّي من جديد
# (تفرضه بوّابة migration-linter، P4-1). لا يدخل requirements.txt: أداةُ CI لا الإنتاج.
django-migration-linter==6.0.0

# اختبارات المتصفّح (tests/e2e/ + بوّابة axe-core الحيّة، P2-1) — playwright
# نفسُه في requirements.txt، لكنّ إضافة pytest عليه أداةُ اختبارٍ لا إنتاج.
pytest-playwright==0.9.0
axe-playwright-python==0.1.8
