---
description: شغّل خادمَ هذه الشجرة على منفذٍ حرّ (قاعدةٌ + خادمٌ + فحصُ /health/)
allowed-tools: Bash(bash scripts/session-up.sh:*)
---

شغّل `bash scripts/session-up.sh $ARGUMENTS` ثمّ أبلغ بالرابط وحده. إن فشل فاقرأ الخطأ وأصلح سببَه (Docker متوقّف؟ الحزمةُ الأصليّة لا تعمل؟) ولا تُعِد المحاولةَ عمياء.
