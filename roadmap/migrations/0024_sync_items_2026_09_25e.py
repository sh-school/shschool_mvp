"""مزامنةُ الخارطة الثانية عشرة (2026-09-25): #589 — SCH-10 مؤشّرُ تركّز الاستثناءات في مختبر الجدول (منجَز بعد حفظ الأساس المرجعيّ).

أرسلتها جلسةُ الجدولة 8033، وتحقّقتُ من #589 من نصّه المدموج (دُمج 2026-09-25 05:34Z، main@74d89bcb). ونُشر على الإنتاج ضمن main@db33f87
(نحو 08:50 الدوحة) بحسب جلسة النشر.

حارسةٌ كسابقاتها: لا تلمس بنداً إلّا إن كانت حالتُه وتقدّمُه ما في آخر لقطة؛ والملاحظةُ الموجودة لا تُلحَق ثانيةً؛ ورمزُ الطلب يُضاف
إلى الحقل لا يستبدله؛ ولا تُغيَّر مواعيد.

- **SCH-10 → منجز** بـ#589: معيارا الإغلاق في وثيقة الخطّة تحقّقا — (أ) مؤشّرٌ في المختبر، و(ب) أساسٌ مرجعيٌّ محفوظٌ على الجدول المعتمد
  (حُفظ على الإنتاج بإذن المالك المباشر نحو 09:00 الدوحة بحسب 8033). وكان 50% قبل حفظ الأساس (100 + 0) / 2 بحسابها الصريح.
- **ملاحظةٌ على SCH-08** بتصحيح 8033: الجدولُ المعتمد الآن على الإنتاج هو التوليد 15007906 (ولّده المالكُ 2026-09-25 01:35 الدوحة) وأُرشف
  c2dba53a الذي سمّته ملاحظاتٌ سابقة معتمَداً؛ وتحقّقُ الأحد يقارن جلساتِ الأحد بالحاليّ.
- **SCH-19 بندٌ مقترَحٌ مفتوحٌ جديد** (اقترحته 8033 وسجّله المالكُ مقترَحاً مفتوحاً بلا تاريخٍ ولا حالةٍ منجزة): تصحيحُ
  teacher.run_breaches القائم الذي يعدّ الحصّةَ المزدوجةَ المطلوبة تتابعاً مخالفاً؛ يغيّر التوليدَ فيُقاس على نسخة الإنتاج قبل الدمج.

ولا يتغيّر SK* بقياس SCH-10 (بحسب 8033). وثُبِّت الأساسُ الجديد بقرار المالك نحو 09:15 الدوحة (بحسب 8033) — ملاحظةٌ لا قرارٌ رسميّ في القرارات.
"""

import datetime

from django.db import migrations

STAMP = "[2026-09-25]"
DAY = datetime.date(2026, 9, 25)

# (الرمز، (الحالة، التقدّم) المتوقَّعان، الحالة الجديدة، التقدّم الجديد، رمزُ الطلب يُضاف، سطر الملاحظة)
UPDATES = [
    (
        "SCH-10",
        ("todo", 0),
        "done",
        100,
        "#589",
        "أُغلق: #589 (8033، main@74d89bcb، 2026-09-25 05:34Z؛ **منشورٌ على الإنتاج** main@db33f87 نحو 08:50 الدوحة — الوحدةُ "
        "operations.schedule_lab_exceptions تُستورد وأمرُ schedule_lab يعمل): مؤشّرُ تركّز الاستثناءات في المختبر `fairness.exception_load` — أكبرُ "
        "ما يقع منها على جهةٍ واحدة، وتفصيلُه يسمّي أثقلَ ثلاثةِ معلّمين وأثقلَ ثلاثِ شُعب؛ والاستثناءُ بعد قرار المالك D-17 ما بقي للمولّد أن "
        "يكسره: تتابعُ **مهمّتين** لمعلّمٍ فوق سقفه في يومه (HC5، بالساعة لا بالرقم فالفسحةُ والصلاةُ تفصلان) وتجاورُ حصّتَي مادّةٍ في شعبةٍ في "
        "يومٍ واحد (HC20). **عرضٌ لا حكم:** لا يدخل الدرجةَ التي تقود المولّدَ والمحسِّن ولا يكلّف محاولةً (اختبارٌ يحرس أنّ الدرجةَ بلا "
        "المؤشّر هي الدرجةُ معه)، و10 حالاتِ اختبارٍ جديدة، وحدةٌ مستقلّة operations/schedule_lab_exceptions.py لأنّ ملفَّ المختبر بلغ حدَّ "
        "الحجم. **تحقّق معيارا الإغلاق في وثيقة الخطّة:** (أ) مؤشّرٌ في المختبر ✔ منجَزٌ ومدموجٌ ومنشور؛ (ب) **أساسٌ مرجعيٌّ محفوظٌ على الجدول "
        "المعتمد ✔** — نُفِّذ بإذن المالك المباشر (نحو 09:00 الدوحة بحسب 8033) بأمرٍ واحدٍ على الإنتاج (schedule_lab --generation … --store) على "
        "التوليد المعتمد الآن 15007906: `fairness.exception_load` = **2** (أقصى ما وقع على معلّمٍ واحد يومان، و6 أيّامِ معلّمين استثناءً في "
        "المجموع، و0 على الشعب)؛ والأساسُ صفٌّ جديدٌ **غيرُ مثبَّت** «أساس 2026-09-25 — التوليد 15007906» (ScheduleBaseline d1094e40)؛ "
        "وعند حفظه لم يُمسّ الأساسُ المثبَّتُ «أساس 2026-2027 المعتمَد» (مثبَّتٌ واحدٌ قبلُ وبعد؛ الأسسُ 5 ← 6). وكان البند 50% قبل حفظ الأساس بحسابها "
        "الصريح (100 + 0) / 2. **ثُبِّت الأساسُ الجديد بقرار المالك** (نحو 09:15 الدوحة، بحسب 8033 وبقوله المباشر لها «ثبّت الأساس الجديد»؛ أمر schedule_lab --pin على الإنتاج): صار المرجعُ المعتمَد في مختبر الجدول للعام 2026-2027 «أساس 2026-09-25 — التوليد 15007906» (26 مؤشّراً) بعد «أساس 2026-2027 المعتمَد» (d37abbc3، من 09-10) الذي بقي غيرَ مثبَّتٍ ويمكن إعادةُ تثبيته؛ فالدرجةُ المنسوبةُ في المختبر (100 = «كما اعتمدنا») تُقاس الآن على الجدول المعتمد 15007906 لا على جدول 09-10 (مثبَّتٌ واحدٌ قبلُ وبعد). ولا تغيّرَ في SK* "
        "بحسب 8033. القياسُ بأداة schedule_lab على الإنتاج بتاريخ 2026-09-25؛ وقبله على قاعدة الجلسة (نسخةٌ من الإنتاج): 6 أيّامِ معلّمٍ "
        "استثناءً في الجدول المعتمد المحلّيّ وأقصاها يومان، ومسودّةٌ 7 وأقصاها يوم. بلا هجرات.",
    ),
    (
        "SCH-08",
        ("doing", 90),
        "doing",
        90,
        "",
        "تصحيحٌ من 8033 (2026-09-25): الجدولُ المعتمد الآن على الإنتاج هو التوليد 15007906 (ولّده المالكُ 2026-09-25 01:35 بالدوحة، 869 حصّة، "
        "7 مخالفات، جودة 80.6) وأُرشف c2dba53a الذي سمّته الملاحظاتُ السابقة معتمَداً. فتحقّقُ الأحد 2026-09-27 يقارن جلساتِ الأحد بالحاليّ "
        "15007906؛ والبندُ يبقى 90 حتى نتيجته.",
    ),
]


# بندٌ مقترَحٌ مفتوحٌ بلا تاريخٍ (سجّله المالكُ كذلك): (الرمز، المسار، العنوان، الاعتماديّات، معيار الإغلاق، ملاحظة)
PROPOSED_ITEMS = [
    (
        "SCH-19",
        "backend",
        "تصحيحُ teacher.run_breaches: المزدوجةُ المطلوبةُ (التربية الفنّية والمختبرات) مهمّةٌ واحدة لا تتابعٌ مخالف",
        "SCH-08",
        "teacher.run_breaches يعدّ المزدوجةَ المطلوبةَ مهمّةً واحدةً كما يحكم HC5 في المولّد، ويُقاس أثرُه على التوليد على نسخة الإنتاج "
        "(معاملةٌ تُتراجع) قبل الدمج؛ ومعيارُ الإغلاق التفصيليّ في docs/schedule_generation_remediation_plan_2026-09.md.",
        "اقترحته 8033 عند دمج #589 وسجّله المالكُ بنداً مقترَحاً مفتوحاً بلا تاريخٍ (تحت مظلّة SCH، مسار backend). اكتُشف بقياس #589: "
        "teacher.run_breaches القائم يعدّ المزدوجةَ المطلوبةَ (التربية الفنّية والمختبرات) تتابعاً مخالفاً — في المختبر 24 يومَ معلّمٍ (6.9%) "
        "والحقيقيُّ 6 (نحو 1.7%) على قاعدة الجلسة (نسخةٌ من الإنتاج). يدخل الدرجةَ التي تقود المحسِّن فتصحيحُه **يغيّر التوليد**. لا يحجبه "
        "قرارُ مالك، لكنّ 8033 لن تبدأه قبل تحقّق الأحد 2026-09-27 (SCH-08). الجهدُ غيرُ مقدَّر (القيمةُ الافتراضيّة).",
    ),
]
PROPOSED_FIRST_ORDER = 619  # بعد SCH-18 (618)
PROPOSED_BASIS = "غير مجدول — اقترحته جلسة الجدولة 8033 وسجّله المالك مقترَحاً مفتوحاً"


def _add_pr(existing, pr):
    """تُضيف رموزَ الطلبات الجديدة إلى ما في الحقل بلا تكرار."""
    return " ".join(dict.fromkeys([*existing.split(), *pr.split()]))


def sync(item_model):
    """تُطبِّق تحديثاتِ البنود الحارسة؛ تُرجع رموزَ ما تغيّر."""
    changed = []
    for code, expected, status, progress, pr, line in UPDATES:
        item = item_model.objects.filter(code=code).first()
        if item is None or (item.status, item.progress) != expected or line in item.note:
            continue
        item.status, item.progress = status, progress
        if pr:
            item.pr = _add_pr(item.pr, pr)
        item.note = f"{item.note}\n{STAMP} {line}".strip()
        item.save(update_fields=["status", "progress", "pr", "note", "updated_at"])
        changed.append(code)
    return changed


def add_proposed(item_model):
    """تُنشئ البنودَ المقترَحةَ الغائبة مفتوحةً بلا تاريخ؛ تُرجع رموزَ ما أُنشئ."""
    created = []
    for offset, (code, lane, title, deps, criterion, note) in enumerate(PROPOSED_ITEMS):
        if item_model.objects.filter(code=code).exists():
            continue
        item_model.objects.create(
            code=code,
            src="SCH",
            lane=lane,
            title=title,
            status="todo",
            progress=0,
            date_basis=PROPOSED_BASIS,
            deps=deps,
            criterion=criterion,
            note=f"{STAMP} {note}",
            sort_order=PROPOSED_FIRST_ORDER + offset,
        )
        created.append(code)
    return created


def forwards(apps, schema_editor):
    item_model = apps.get_model("roadmap", "RoadmapItem")
    # قاعدةٌ بلا استيراد (اختبار، شجرةٌ جديدة): لا شيء يُزامَن، ولا بنودٌ يتيمة.
    if not item_model.objects.exists():
        return
    sync(item_model)
    add_proposed(item_model)


class Migration(migrations.Migration):
    dependencies = [
        ("roadmap", "0023_sync_items_2026_09_25d"),
    ]

    operations = [
        migrations.RunPython(forwards, migrations.RunPython.noop),
    ]
