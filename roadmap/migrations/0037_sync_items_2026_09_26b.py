"""الخارطةُ الرابعةُ والعشرون (2026-09-26): مراجعةُ البنود المتأخّرة بتأكيد المالك المباشر، وإعادةُ تعريف DBT-11، وقراءاتُ Q-10 وVI-25.

**مؤكَّدٌ من المالك مباشرةً لجلسة الخارطة (2026-09-26، في المحادثة وبأسئلةٍ صريحة):**
- **SCH-06 → مؤجَّل:** توليدٌ كاملٌ جديدٌ في الإنتاج بعد النشر لا إصلاحُ الجدول الحيّ (كان قرارَ 09-24 منقولاً عن جلسة الجدولة، فصار مؤكَّداً).
- **OWN-02:** مؤجَّلٌ بانتظار تنفيذ المالك بيده، بلا موعد.
- **DBT-14 يُغلق** بقراءة Sentry (مقيسةً في هذه الجلسة).
- **DBT-11 يُعاد تعريفُه** بعد أن تحقّق مسارُ الأمن والامتثال من الشيفرة أنّ نصَّه الأصليَّ لا تسنده (payload الـDLQ معرّفاتٌ فقط)، ويُحسم قراران: **D-27** الرسائلُ الموسومةُ «غير مسلَّمة» في غياب المزوّد لا تُعاد بعد شرائه؛ **D-28** سقفُ بقاء DLQ غير المحلول 90 يوماً.
- **إسناد:** Q-10 لمسار الجوال وتجربة المستخدم، وQ-11 لـ«بصريّ · محور الجوال»، والشقُّ البرمجيّ من DBT-11 لـ«منصّة · فحص الإدارة» (موعدُه 09-30).

**ما اندمج أو أُنجز:** #655 (VI-25) اندمج؛ Q-10 أُنجز بلا طلب دمج (سكربتُ قراءةٍ فقط شغّله المالكُ على الإنتاج).
**ما لم يندمج ويُسجَّل بحالته:** #671 (الشقُّ البرمجيّ من DBT-11) مفتوحٌ لم يندمج.

حارسةٌ كسابقاتها: لا يُلمس بندٌ ولا مؤشّرٌ إلّا إن طابقت حالتُه وقيمتُه آخرَ لقطة، ولا تُكرَّر ملاحظةٌ، ورمزُ الطلب يُضاف بحدّ الحقل 64، ويُنشأ الغائبُ وحدَه.
والمستودعُ عامّ: نصوصٌ محايدة. **لا يُغلق بندٌ بلا قياس:** ما لم يُتحقَّق يُذكر في ملاحظته.
"""

import datetime

from django.db import migrations

STAMP = "[2026-09-26]"
DAY = datetime.date(2026, 9, 26)
D = datetime.date


# (الرمز، (الحالة، التقدّم) المتوقَّعان، الحالة الجديدة، التقدّم الجديد، رفعُ البوّابة، رمزُ الطلب يُضاف، سطر الملاحظة،
#  تواريخ: None أو ((بدايةٌ متوقَّعة، نهايةٌ متوقَّعة)، (بدايةٌ جديدة، نهايةٌ جديدة، أساسُ التاريخ)))
UPDATES = [
    (
        "DBT-14",
        ("blocked", 0),
        "done",
        100,
        False,
        "",
        "**DBT-14 يُغلق بقراءة Sentry (مقيسةً في جلسة الخارطة، 2026-09-26):** قضيّتا الخطأ (SCHOOLOS-PRODUCTION-29 بـ2,308 حدثاً وSCHOOLOS-PRODUCTION-2A بـ79) وقعتا كلتاهما في نافذةٍ "
        "واحدةٍ يومَ 2026-09-17 من 06:17 إلى 09:46 UTC (بحثُ 30 يوماً)، ولا حدثَ بعدها منذ تسعة أيّام — **فالتكرارُ توقّف**، وهو ما كان ينتظره البند من المالك. **تحفّظ:** لم يثبت "
        "السببُ الجذريّ (كان تشخيصُ 09-23 أنّ /health/ يفحص الخطَّ عمداً وأنّ الأحداث من نشراتٍ سابقة — أرجحٌ لا مثبَت). **لم تُحلّ القضيّتان في Sentry** (كتابةٌ خارجيّة — للمالك إن أراد). "
        "وقضيّةٌ منفصلةٌ (`js/actions.js`، ثلاثةُ أحداثٍ آخرُها 2026-09-25 11:14 UTC، قبل نشر N-042 بنحو ست ساعات) لا حدثَ بعدها.",
        None,
    ),
    (
        "SCH-06",
        ("doing", 80),
        "deferred",
        80,
        False,
        "",
        "**مؤجَّلٌ بتأكيد المالك المباشر (2026-09-26):** توليدٌ كاملٌ جديدٌ في الإنتاج بعد النشر لا إصلاحُ الجدول الحيّ — وهو ما نُقل عن جلسة الجدولة يومَ 2026-09-24 مسائيّاً "
        "وبقي غيرَ مؤكَّدٍ لجلسة الخارطة حتى اليوم. فالنسبةُ 80% تبقى كما هي (مكتوبٌ ومختبَرٌ وحدةً #548 وباقٍ في الشيفرة)، ولا يُعاد فتحُه إلّا بقرار.",
        None,
    ),
    (
        "OWN-02",
        ("todo", 0),
        "deferred",
        0,
        False,
        "",
        "**مؤجَّلٌ بانتظار تنفيذ المالك بيده (قرارُه المباشر، 2026-09-26): بلا موعد.** يشغّل seed_quality_templates معاينةً ثمّ --apply بنفسه؛ وجلسةُ النشر تجهّز الأوامرَ فقط ولا "
        "تُكتب كتابةُ إنتاجٍ من الخارطة. شرطاه (نشرُ #336 و#477) متحقّقان منذ 2026-09-25. البوّابةُ «مالك» باقية.",
        (
            (D(2026, 9, 22), D(2026, 9, 23)),
            (None, None, "بانتظار تنفيذ المالك بيده — قرارُه 2026-09-26"),
        ),
    ),
    (
        "VI-25",
        ("todo", 0),
        "done",
        100,
        True,
        "#655",
        "**VI-25 منجز (#655 اندمج؛ «بصريّ · تخطيط لوحة المدير»، 2026-09-25):** آخرُ عقدة axe — رابطُ «غير المقروء فقط» يحمل `aria-current` لا `aria-pressed`. **المقيس (صاحبُ الطلب):** "
        "عقدةُ axe 1 ← 0 على 8 تركيبات (نهارٌ/ليل × عروض)، وخطُّ أساس السقّاطة صار {}. **لم يُتحقَّق:** خضرةُ وظيفة axe على main بعد الدمج ولا نشرُه. وقد سُجّلت قراءةُ V-A1 وPK9.",
        None,
    ),
    (
        "Q-10",
        ("todo", 0),
        "done",
        100,
        False,
        "",
        "**Q-10 منجز بتحفّظٍ عن معيار الصفحات — بلا طلب دمج (2026-09-26):** سكربتُ تجميعٍ قراءةٌ فقط خارج المستودع شغّله المالكُ بنفسه على الإنتاج؛ (مسار الجوال وتجربة المستخدم). "
        "**المقيس (دخولاتُ action=login في سجلّ المراجعة، نافذةُ 60 يوماً من 2026-08-10 إلى 2026-09-26):** 525 دخولاً من 36 مستخدماً متميّزاً؛ **الهاتف 10.9% من الدخولات (57 من 525) و50.0% "
        "من المستخدمين (18 من 36)**؛ الحاسوب 88.2% من الدخولات و88.9% من المستخدمين (يتداخلان)؛ لا لوحيَّ مصنَّفاً؛ المتصفّح: Edge 87.6% وSafari 6.9% (9 مستخدمين) وChrome 4.2% (12)؛ "
        "على الهاتف نحو 9 مستخدمين iOS ونحو 9 Android. لا IP ولا مستخدم ولا هويّةٌ خام؛ والخلايا دون 5 مخفيّة. **حدود:** العيّنةُ 36 مستخدماً لا تمثّل الانتشارَ كلَّه؛ وiPad على "
        "Safari الحديث يُعدّ حاسوباً؛ وأرقامُ نظام الهاتف غيرُ موثوقةٍ (تجميدُ Android 10 وiOS 18_x) فلا تفصيلَ بنسخ؛ و**لا سجلَّ لزيارات الصفحات** فمعيارُ «أكثر الصفحات زيارةً جوّالاً» "
        "لم يتحقّق وعُدِّل. **الأثرُ على القرارات (اقتراحُ صاحب المسار لا حسم):** D10/MD10 لا تُحسم بنسخٍ من هذه البيانات فيبقى «آخرُ نسختين رئيسيّتين» افتراضاً؛ D4/MD4 لا يتغيّر؛ MD2 لا يُعكس "
        "لكنّ أولويّةَ M-12 تنخفض أمام Q-06 وQ-07؛ وأيُّ تفصيلٍ لاحقٍ من نسخ المتصفّح في Q-04 (RUM).",
        None,
    ),
    (
        "Q-11",
        ("todo", 0),
        "doing",
        0,
        False,
        "",
        "**بدأ (2026-09-26) بتأكيد مالك «بصريّ · محور الجوال»:** لا مقارِنَ ثانياً — تُوسَّع مصفوفةُ مقارِن VI-13 (SHOTS بياناتٌ فقط؛ بموافقة صاحب VI-13 وبلا تعارض) بصفحات H-03 (شبكةُ "
        "الحضور، كشفُ الشعبة، المؤشّرات، التفريغات، الجدولُ الأسبوعيّ…) ورحلتَين متعدّدتَي الخطوات على الجوال (لوحةُ الأوامر، قائمةُ الهامبرغر) بحقل خطواتٍ حتميّ؛ فيُدمج Q-11 عمليّاً "
        "في مسار VI-13 إضافةً. الدافعُ: H-03 (#664، مفتوحٌ عند الكتابة) يرفع تصريحاتٍ في الشبكات الكثيفة إلى 12px بلقطات قبل/بعد يدويّةٍ اعتمدها المالك، ولو وُجد الحارسُ البصريّ لوفّر المراجعة اليدويّة. "
        "**الموعدُ الذي التزم به المقعد: طلبُ دمجٍ مفتوحٌ اليوم 2026-09-26.** لا طلبَ بعدُ؛ وتشغيلُ الوظيفة الحيّة على الطلب (visual-snapshots.yml) شرطُ الإثبات الطرفيّ وهو عند صاحب VI-13 "
        "أو جلسة النشر. **لم يُقَس شيءٌ بعدُ.**",
        (
            (D(2026, 9, 23), D(2026, 9, 24)),
            (D(2026, 9, 26), D(2026, 9, 26), "التزامُ المقعد 2026-09-26: طلبُ دمجٍ مفتوحٌ اليوم"),
        ),
    ),
    (
        "DBT-11",
        ("todo", 0),
        "doing",
        33,
        False,
        "",
        "**التقدّم 33% = خطوتان من ستّ (التعريفُ الجديدُ والشيفرةُ والاختباراتُ في طلبٍ مفتوح #671 ✓؛ الدمجُ والنشرُ وقياسُ الإنتاج ومعاينةُ Sentry ✗) — اشتقاقٌ لا قياس؛ ولا يُغلق قبلها.** "
        "**#671 مفتوحٌ لم يندمج** («منصّة · فحص الإدارة»، بلا هجرة): بلا مزوّدٍ يُوسَم التسليمُ undeliverable قبل الاستحواذ بلا إعادةٍ ولا DLQ ولا exception (تحذيرٌ واحد)، وأعطالُ المزوّد "
        "الحقيقيّ تبقى إعادةً فـDLQ؛ وقائمةٌ بيضاء لمفاتيح DLQ (`student_id` و`user_id` و`sent_by_id` و`dispatch_id` و`notif_type` و`reason`) تُنقّي بها الكتابةُ مع فحص AST. "
        "**المقيس محلّيّاً (2026-09-26):** الاختباراتُ أُثبتت بتحويرٍ سقط به عشرةُ اختبارات، و671 اختباراً ذا صلة تمرّ، وmypy 1.10.0 وruff نظيفان. **لم يُقَس:** الإنتاج — يلزم عددُ صفوف DLQ "
        "حسب النوع والحالة وجردُ مفاتيح payload (قراءةٌ فقط بيد المالك أو جلسة النشر) لمعرفة إن وُجد مفتاحُ محتوىً في صفوفٍ قديمة. وحذفُ DLQ بعد 90 يوماً (D-28) طلبٌ لاحقٌ بمعاينة.",
        (
            (D(2026, 9, 22), D(2026, 9, 24)),
            (
                D(2026, 9, 26),
                D(2026, 9, 30),
                "التزامُ «منصّة · فحص الإدارة» 2026-09-26: 09-30 (الطلبُ #671 مفتوح)",
            ),
        ),
    ),
]

# إعادةُ تعريف بندٍ بقرار المالك ما دام عنوانُه ومعيارُه كما في آخر لقطة (يبقى القديمُ في الملاحظة):
# (الرمز، العنوانُ القديم بالضبط، العنوانُ الجديد أو None، المعيارُ القديم بالضبط، المعيارُ الجديد، سطرُ الملاحظة)
REDEFINES = [
    (
        "DBT-11",
        "بريدٌ لا يُسلَّم يدخل الإعادة ثم DLQ ثم Sentry والـDLQ يحفظ نصّ الرسالة (أسماء طلبة وأولياء) في القاعدة — أثرٌ جانبيّ لـ#389",
        "بريدٌ لا يُسلَّم في غياب المزوّد لا يدخل إعادةً ولا DLQ ولا Sentry، ولا يبقى في DLQ محتوىً",
        "حين لا مزوّد فعليّ: تُوسَم الرسالة undeliverable بلا إعادة ولا DLQ ولا Sentry، وتُعاد بعد شراء المزوّد؛ ولا نصّ شخصيّ في DeadLetterMessage",
        "حين لا مزوّد فعليّ: تُوسَم الرسالةُ undeliverable نهائيّاً بلا إعادةٍ ولا DLQ ولا Sentry (تحذيرٌ واحد) ولا تُعاد بعد شراء المزوّد (D-27)؛ ومفاتيحُ payload في "
        "DeadLetterMessage من قائمةٍ بيضاء لا مفتاحَ محتوىً فيها (حارسٌ بنيويّ)؛ وقياسُ الإنتاج (صفوفُ DLQ حسب النوع والحالة وجردُ المفاتيح) بلا مفتاحِ محتوىً؛ وسقفُ بقاء "
        "DLQ غير المحلول 90 يوماً (D-28، طلبٌ لاحق).",
        "**أُعيد تعريفُ البند بتأكيد المالك المباشر (2026-09-26):** تحقّق مسارُ الأمن والامتثال من الشيفرة أنّ النصَّ الأصليَّ لا تسنده — payload الـDLQ معرّفاتٌ فقط (`student_id` "
        "و`notif_type` و`sent_by_id`) بعقدٍ موثَّقٍ أقدمَ من #389، ونصُّ الخطأ يُنقّى قبل التخزين، وSentry يحجب متغيّراتِ الإطار بالمفتاح (اختباراتٌ قائمة). **المشكلةُ الفعليّة:** بلا "
        "مزوّدٍ فعليّ كلُّ رسالةٍ تدخل إعادةً ثمّ DLQ ثمّ حدثَ Sentry (ضجيجٌ وتراكمٌ لا تسريبُ نصّ)، وصفوفُ DLQ غيرُ المحلولة لا تُحذف بمرور الزمن. **لم يُقَس:** صفوفٌ قديمةٌ "
        "سابقةٌ لإصلاح العقد قد تحمل محتوىً (يلزمه قراءةُ الإنتاج). **النصُّ القديم:** «بريدٌ لا يُسلَّم يدخل الإعادة ثم DLQ ثم Sentry والـDLQ يحفظ نصّ الرسالة (أسماء طلبة وأولياء) "
        "في القاعدة — أثرٌ جانبيّ لـ#389»؛ ومعيارُه «… وتُعاد بعد شراء المزوّد؛ ولا نصّ شخصيّ في DeadLetterMessage».",
    ),
    (
        "Q-10",
        None,
        None,
        "جدولٌ مجمَّعٌ: حصّةُ الجوال، أكثرُ الصفحات زيارةً جوّالاً، نسخُ iOS/Android",
        "جدولٌ مجمَّعٌ من دخولات سجلّ المراجعة (لا زياراتِ الصفحات): حصّةُ الجوال من الدخولات ومن المستخدمين، ونظامُ الهاتف وفئةُ المتصفّح (بلا نسخٍ موثوقة)؛ والخلايا دون 5 مخفيّة",
        "**عُدِّل المعيارُ (2026-09-26، اقتراحُ مسار الجوال وتحقّق):** لا سجلَّ لزيارات الصفحات فلا «أكثر الصفحات زيارةً جوّالاً»، ونسخُ الأنظمة غيرُ موثوقةٍ من الهويّة. "
        "القديم: «جدولٌ مجمَّعٌ: حصّةُ الجوال، أكثرُ الصفحات زيارةً جوّالاً، نسخُ iOS/Android».",
    ),
]

# قراراتٌ محسومةٌ بتأكيد المالك المباشر: (الرمز، المصدر، القرار، يحجب، الخيارات، التوصية، تاريخ الحسم، الترتيب)
NEW_DECISIONS = [
    (
        "D-27",
        "DBT",
        "الرسائلُ الموسومةُ «غير مسلَّمة» في غياب مزوّد البريد: لا تُعاد بعد شراء المزوّد",
        "DBT-11 (معيارُه) وشراءُ المزوّد D-01",
        "(أ) لا تُعاد — الوسمُ نهائيّ. (ب) تُعاد بعد شراء المزوّد (يقتضي حفظَ نصّها مشفّراً).",
        "(أ) — حسمه المالكُ لجلسة الخارطة مباشرةً (2026-09-26) بتوصية مسار الأمن والامتثال: الإعادةُ تقتضي حفظَ نصّ الرسالة مشفّراً وهو ما يمنعه عقدُ payload الـDLQ (مبدأ "
        "التقليل)، والإشعاراتُ الداخليّة تصل أصلاً.",
        D(2026, 9, 26),
        141,
    ),
    (
        "D-28",
        "DBT",
        "سقفُ بقاء صفوف DLQ غير المحلولة: 90 يوماً بعد الإنشاء مع تصعيدٍ قبله ثمّ حذفٌ بمعاينة",
        "DBT-11 (الطلبُ اللاحق للحذف)",
        "(أ) 90 يوماً. (ب) مدّةٌ أخرى يحدّدها المالك. (ج) لا يُحسم الآن.",
        "(أ) — حسمه المالكُ لجلسة الخارطة مباشرةً (2026-09-26) بتوصية مسار الأمن والامتثال. حالياً صفوفُ DLQ غيرُ المحلولة لا تُحذف بمرور الزمن؛ والحذفُ طلبٌ لاحقٌ بمعاينةٍ بأعداد.",
        D(2026, 9, 26),
        142,
    ),
]

# قراءةُ مؤشّرٍ لم يُمسّ منذ آخر لقطة: (الرمز، (القيمة، تاريخ القياس) المتوقَّعان، القيمة الجديدة، مرجعُ القياس يُلحق بالمصدر)
KPI_UPDATES = [
    (
        "MK26",
        (None, None),
        10.9,
        "Q-10، 09-26: هاتفٌ 10.9% من الدخولات (57 من 525) و50% من المستخدمين (18 من 36)؛ دخولاتٌ لا زيارات، 60 يوماً، معلوماتيّ",
    ),
    (
        "V-A1",
        (1.0, D(2026, 9, 25)),
        0.0,
        "بعد #655 («بصريّ · تخطيط لوحة المدير»): axe 1 ← 0 على 8 تركيبات؛ قياسُ الطلب قبل الدمج",
    ),
    ("PK9", (1.0, D(2026, 9, 25)), 0.0, "مرآة V-A1 (#655، axe 1 ← 0 على 8 تركيبات)"),
]


def _add_pr(existing, token):
    """تُضيف رمزَ الطلب دون تكرارٍ ودون تجاوز حدّ الحقل 64 (وإلّا يبقى الحقلُ كما هو)."""
    if not token:
        return existing
    joined = " ".join(dict.fromkeys([*existing.split(), *token.split()]))
    return joined if len(joined) <= 64 else existing


def sync(item_model):
    """تُطبِّق تحديثاتِ البنود الحارسة؛ تُرجع رموزَ ما تغيّر."""
    changed = []
    for code, expected, status, progress, clear_gate, pr, line, dates in UPDATES:
        item = item_model.objects.filter(code=code).first()
        if item is None or (item.status, item.progress) != expected or line in item.note:
            continue
        if dates is not None and (item.start_date, item.end_date) != dates[0]:
            continue
        item.status, item.progress = status, progress
        item.pr = _add_pr(item.pr, pr)
        fields = ["status", "progress", "pr", "gate", "note", "updated_at"]
        if clear_gate:
            item.gate = ""
        if dates is not None:
            item.start_date, item.end_date, item.date_basis = dates[1]
            fields += ["start_date", "end_date", "date_basis"]
        item.note = f"{item.note}\n{STAMP} {line}".strip()
        item.save(update_fields=fields)
        changed.append(code)
    return changed


def redefine_items(item_model):
    """تُعيد تعريفَ بندٍ ما دام عنوانُه ومعيارُه كما في آخر لقطة وتُبقي القديمَ في ملاحظته؛ تُرجع رموزَ ما تغيّر."""
    changed = []
    for code, old_title, new_title, old_criterion, new_criterion, line in REDEFINES:
        item = item_model.objects.filter(code=code).first()
        if item is None or item.criterion != old_criterion or line in item.note:
            continue
        if old_title is not None and item.title != old_title:
            continue
        if new_title is not None:
            item.title = new_title
        item.criterion = new_criterion
        item.note = f"{item.note}\n{STAMP} {line}".strip()
        item.save(update_fields=["title", "criterion", "note", "updated_at"])
        changed.append(code)
    return changed


def add_decisions(decision_model):
    """تُنشئ القراراتِ المحسومةَ الغائبة بتأكيد المالك؛ لا تُعيد كتابةَ قرارٍ موجود؛ تُرجع رموزَ ما أُنشئ."""
    created = []
    for code, src, title, blocks, options, recommendation, decided_on, order in NEW_DECISIONS:
        if decision_model.objects.filter(code=code).exists():
            continue
        decision_model.objects.create(
            code=code,
            src=src,
            title=title,
            status="decided",
            decider="المالك",
            due="",
            decision_date=decided_on,
            blocks=blocks,
            options=options,
            recommendation=f"{STAMP} {recommendation}",
            sort_order=order,
        )
        created.append(code)
    return created


def _record(kpi, value):
    """تكتب قياسَ يومه في السجلّ: تستبدل نقطةَ اليوم إن وُجدت، وإلّا تُلحقها."""
    today = DAY.isoformat()
    history = [point for point in (kpi.history or []) if point.get("d") != today]
    history.append({"d": today, "v": value})
    kpi.current, kpi.measured_at, kpi.history = value, DAY, history


def _with_source(kpi, note):
    """تُلحق مرجعَ القياس بالمصدر دون تكرارٍ وبحدّ الحقل 255 (وإلّا يبقى المصدرُ كما هو)."""
    if not note or note in kpi.source:
        return kpi.source
    source = f"{kpi.source} ({note})".strip()
    return source if len(source) <= 255 else kpi.source


def sync_kpis(kpi_model):
    """تُحدِّث المؤشّراتِ التي لم تُمسّ منذ آخر لقطة؛ تُرجع رموزَ ما تغيّر."""
    changed = []
    for code, expected, value, note in KPI_UPDATES:
        kpi = kpi_model.objects.filter(code=code).first()
        if kpi is None or (kpi.current, kpi.measured_at) != expected:
            continue
        source = _with_source(kpi, note)
        _record(kpi, value)
        kpi.source, kpi.why = source, ""
        kpi.save(update_fields=["current", "measured_at", "history", "source", "why", "updated_at"])
        changed.append(code)
    return changed


def forwards(apps, schema_editor):
    item_model = apps.get_model("roadmap", "RoadmapItem")
    # قاعدةٌ بلا استيراد (اختبار، شجرةٌ جديدة): لا شيء يُزامَن، ولا بنودٌ يتيمة.
    if not item_model.objects.exists():
        return
    # إعادةُ التعريف قبل التحديث: يطابق الحارسُ العنوانَ والمعيارَ كما في آخر لقطة.
    redefine_items(item_model)
    sync(item_model)
    add_decisions(apps.get_model("roadmap", "RoadmapDecision"))
    sync_kpis(apps.get_model("roadmap", "RoadmapKpi"))


class Migration(migrations.Migration):
    dependencies = [
        ("roadmap", "0036_sync_items_2026_09_26"),
    ]

    operations = [
        migrations.RunPython(forwards, migrations.RunPython.noop),
    ]
