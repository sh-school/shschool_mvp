"""الخارطةُ الخامسةُ والعشرون (2026-09-26): عائلةُ «ساعة المدرسة» CLK-01..03 وبندُ جدول المناوبة DUT-01 — مقترَحةٌ بلا مواعيد بتأكيد المالك.

**مؤكَّدٌ من المالك مباشرةً لجلسة الخارطة (2026-09-26، في المحادثة):** تسجيلُ الفكرتَين كما اقترحتُ — بنودٌ `todo` بلا مواعيد وبأساس «مقترَح»، وقراراتُ المدير التي نقلها المالكُ عبر «جدول · التشغيل» تُكتب **ملاحظاتٍ** لا قراراتٍ محسومة.
**ما ليس فيها:** لا بناءَ ولا طلبَ دمج؛ والاقتراحُ عصفٌ ذهنيٌّ لم يُطلب البناءُ فيه بعد. وجدولُ المناوبة **لا يبدأ قبل ردّ المدير** على أسئلةٍ ثمانية (اعتمد المالكُ ذلك لأمين البلاغات مباشرةً).

حارسةٌ كسابقاتها: يُنشأ الغائبُ وحدَه ولا يُعاد كتابةُ بندٍ موجود. والمستودعُ عامّ: نصوصٌ محايدة بلا أسماءٍ ولا أرقام أشخاص.
"""

from django.db import migrations

STAMP = "[2026-09-26]"

BASIS = "غير مجدول — مقترَحٌ أكّده المالكُ لجلسة الخارطة 2026-09-26؛ بلا بناءٍ حتى يُطلب"
REF = "ساعة المدرسة — اقتراحُ «جدول · التشغيل» 2026-09-26 (عصفٌ ذهنيّ)"
COMMON = (
    "**أكّد المالكُ تسجيلَه مقترَحاً بلا موعد (2026-09-26، مباشرةً لجلسة الخارطة)؛ العصفُ الذهنيّ لم يُطلب البناءُ فيه بعد.** الأصلُ خدمةُ ساعةٍ واحدة تخدم الجداولَ الثلاثة "
    "(العامّ والمفرد والشعبة) لا ثلاثُ ميزات. الجمهورُ (نقلٌ عن المالك): المعلّم، والمدير، والنائبان الأكاديميّ والإداريّ، والمنسّق (قسمُه والجدولُ العامّ)، والمشرفُ الإداريّ، وملاحظُ الطلبة."
)

# بنودٌ جديدة: (الرمز، المصدر، المسار، العنوان، الاعتماديّات، المعيار، الحالة، التقدّم، البداية، النهاية، أساسُ التاريخ، الجهد، البوّابة، طلب الدمج، المرجع، الملاحظة، الترتيب)
NEW_ITEMS = [
    (
        "CLK-01",
        "CLK",
        "product",
        "خدمةُ ساعة المدرسة: مصدرٌ واحدٌ يقرأ الأجراسَ (نطاق/خميس/رمضان) ويتبع الأسبوعَ الفعليّ فيغذّي الجداولَ الثلاثة",
        "",
        "خدمةٌ واحدةٌ تقرأ الأجراسَ وتتبع الأسبوعَ الفعليّ؛ وتعريفُ `bell_tables` واحدٌ لا اثنان (الجناح `wings/services.py` و`operations/schedule_paper.py`)؛ ولا تُبنى ثلاثُ ساعاتٍ منفصلة.",
        "todo",
        0,
        None,
        None,
        BASIS,
        1.0,
        "",
        "",
        REF,
        COMMON
        + " **قراراتٌ إداريّةٌ نقلها المالكُ عن مدير المدرسة عبر «جدول · التشغيل» — لم تصل جلسةَ الخارطة مباشرةً فتُكتب ملاحظاتٍ لا قراراتٍ محسومة:** (1) المنسّقُ يُسند الإشغالَ والتبديلَ كما هو في "
        "المنصّة الآن (فلا تُغيّره الساعة: «فارغو قسمي الآن» يقود إلى مساره القائم)؛ (2) تنبيهاتُ المعلّم ضرورةٌ تنظيميّةٌ لا تجميليّة كي لا يتأخّر عن حصصه — قدرةٌ إلزاميّةٌ موثوقة. **سقط:** «أين الطالب "
        "الآن؟» لا سندَ له في الوثائق (فراغُ سياسة يحتاج قرارَ مالك). **التقاطعات (طلبُ إسنادٍ حين يقرّر المالكُ البدء):** توحيدُ تعريفَي `bell_tables`، ومكوّنُ المؤشّر/العدّاد المركزيّ (الهويّة)، وقناةُ التنبيه "
        "(مسارُ الإشعارات). الجهدُ غيرُ مقدَّر (القيمةُ الافتراضيّة).",
        755,
    ),
    (
        "CLK-02",
        "CLK",
        "product",
        "ساعةُ المدرسة (المرحلة 1): حصّةٌ فعّالةٌ بإطار وعدّادٌ تنازليٌّ إلى الجرس التالي في الجداول العامّ والمفرد والشعبة — بلا تنبيه",
        "CLK-01",
        "الحصّةُ الفعّالةُ بإطارٍ وعدّادٌ يطابق الجرسَ في الجداول الثلاثة؛ المؤشّرُ/العدّادُ مكوّنٌ مركزيٌّ (الهويّة) لا نسخةٌ محلّيّة؛ ولا تنبيهَ في هذه المرحلة.",
        "todo",
        0,
        None,
        None,
        BASIS,
        1.0,
        "",
        "",
        REF,
        COMMON
        + " المرحلةُ 1 بلا تنبيهات (تُبنى تنبيهاتُها في CLK-03). الجهدُ غيرُ مقدَّر (القيمةُ الافتراضيّة).",
        756,
    ),
    (
        "CLK-03",
        "CLK",
        "product",
        "ساعةُ المدرسة (التنبيهات): تنبيهٌ قبل الحصّة وقبل نهايتها بخمس دقائق بقناةٍ موثوقة تُجدوَل على الخادم",
        "CLK-02",
        "التنبيهُ يصل في وقته بلا اعتمادٍ على WebSocket للتوقيت؛ الأصلُ Web Push وجدولةٌ على الخادم؛ ضمن ميزانيّة fanout القائمة؛ وقناةُ التنبيه عند مسار الإشعارات.",
        "todo",
        0,
        None,
        None,
        BASIS,
        1.0,
        "",
        "",
        REF,
        COMMON
        + " الموجودُ اليوم بنيةُ الدفع وميزانيّةُ fanout؛ أمّا الجدولةُ على الخادم للتوقيت فغيرُ موجودة. الجهدُ غيرُ مقدَّر (القيمةُ الافتراضيّة).",
        757,
    ),
    (
        "DUT-01",
        "NEW",
        "product",
        "جدولُ مناوبةٍ للمشرفين الإداريّين ومشرفي الأجنحة (الطابور، الفسحة، الصلاة، الانصراف، الحافلات…) — بعد ردّ مدير المدرسة",
        "CLK-01",
        "ثمانيةُ أسئلةٍ على المدير مجابٌ عنها: وجودُ جدولٍ الآن، ونقاطُ المناوبة وأعدادُها، ومن يُكلَّف، وأسبوعيٌّ أم تدوير، وبديلُ الغائب، والتنبيه، وقائمةُ جولةٍ للمشرف، ومن يقرّر الجرسَ/اليومَ المختصر؛ ثمّ وحدةٌ جديدةٌ بحماية RLS واحتفاظ ومسارات.",
        "todo",
        0,
        None,
        None,
        "بلا موعد — ينتظر ردَّ المدير على ثمانيةِ أسئلة",
        1.0,
        "owner",
        "",
        "SOS-20260926-674D · بطاقة الوصف الوظيفيّ للمشرف الإداريّ («وفق جدول المناوبة»)",
        "**أكّد المالكُ تسجيلَه بلا موعد (2026-09-26، مباشرةً لجلسة الخارطة)، وأكّد لأمين البلاغات مباشرةً: لا بناءَ قبل جواب المدير.** المصدرُ: تذكرةٌ (SOS-20260926-674D) "
        "واقتراحُ «جدول · التشغيل» (عصفُ «ساعة المدرسة»). بطاقةُ الوصف الوظيفيّ للمشرف الإداريّ تنصّ أنّ المناوبةَ في بداية الدوام والفسحة ونهاية الدوام «وفق جدول المناوبة»، ولا جدولَ "
        "له في المنصّة ولا في المدرسة الآن. **البوّابةُ «مالك» = ردُّ المدير** (يرسل المالكُ له الأسئلةَ)؛ ولا مقعدَ منفِّذاً حتى الجواب، وبعده مقعدُ «جدول · التشغيل». وحدةٌ جديدةٌ "
        "(RLS واحد، احتفاظ، مسارات). مرتبطٌ بـCLK-01 (الساعةُ تقرأ المناوبةَ لاحقاً). الجهدُ غيرُ مقدَّر (القيمةُ الافتراضيّة).",
        758,
    ),
]


def add_new_items(item_model):
    """تُنشئ البنودَ الغائبة؛ لا تُعيد كتابةَ بندٍ موجود؛ تُرجع رموزَ ما أُنشئ."""
    created = []
    for (
        code,
        src,
        lane,
        title,
        deps,
        criterion,
        status,
        progress,
        start,
        end,
        basis,
        effort,
        gate,
        pr,
        ref,
        note,
        order,
    ) in NEW_ITEMS:
        if item_model.objects.filter(code=code).exists():
            continue
        item_model.objects.create(
            code=code,
            src=src,
            lane=lane,
            title=title,
            status=status,
            progress=progress,
            start_date=start,
            end_date=end,
            date_basis=basis,
            effort=effort,
            deps=deps,
            criterion=criterion,
            gate=gate,
            pr=pr,
            ref=ref,
            note=f"{STAMP} {note}",
            sort_order=order,
        )
        created.append(code)
    return created


def forwards(apps, schema_editor):
    item_model = apps.get_model("roadmap", "RoadmapItem")
    # قاعدةٌ بلا استيراد (اختبار، شجرةٌ جديدة): لا شيء يُزامَن، ولا بنودٌ يتيمة.
    if not item_model.objects.exists():
        return
    add_new_items(item_model)


class Migration(migrations.Migration):
    dependencies = [
        ("roadmap", "0037_sync_items_2026_09_26b"),
    ]

    operations = [
        migrations.RunPython(forwards, migrations.RunPython.noop),
    ]
