"""الخارطةُ الثالثةُ والعشرون (2026-09-26): ما اندمج بعد 0035، وقرارُ نطاق mypy وبندُ سقّاطة المُسكِتات بتأكيد المالك المباشر.

**مؤكَّدٌ من المالك مباشرةً لجلسة الخارطة (2026-09-25 مساءً):**
- **D-26 (نطاقُ mypy):** تُخرَج أوامرُ الإدارة من نطاق السقّاطة — قرارٌ محسوم؛ وإسنادُ DBT-01 (غيرُ عاجل، قبل 10-16) لأمين المستودع؛ وتغييرُ النطاق **لا يُحتسب تقدّماً** في PK6.
- **DBT-57 (سقّاطةُ قمع المُسكِتات):** بندٌ جديدٌ يتبع DBT-01 بموعدٍ مقترَحٍ 10-09.
- **N-046:** قرارُ «الخادمُ المقيمُ الوحيد» أكّده المالكُ لجلسة الخارطة مباشرةً (كانت عبارةُ 0034 «نقلٌ لم يؤكّده» فتُصحَّح).

**ما اندمج (لا منشورَ إلّا بإشعار جلسة النشر):** #643 (Q-06) و#645 (DBT-46) و#644 و#640 (REP-10) و#646 (VI-13) و#650 (REP-11) و#651 (LAY-03) و#652 (N-048) و#657 (N-047) و#658 (بندٌ جديد N-049: سلّم المخدّرات والأسلحة في دليل السلوك).
**ما لم يندمج ويُسجَّل بحالته:** #656 (LAY-10) مفتوحٌ لا يُنشر قبل استقرار الأحد بطلب المالك.
**اقتراحاتُ مالكي المسارات (لا قياس):** REP-08 وREP-09 وREP-10 وLAY-10 وN-048.

حارسةٌ كسابقاتها: لا يُلمس بندٌ ولا مؤشّرٌ إلّا إن طابقت حالتُه وقيمتُه آخرَ لقطة، ولا تُكرَّر ملاحظةٌ، ورمزُ الطلب يُضاف بحدّ الحقل 64، ويُنشأ الغائبُ وحدَه.
والمستودعُ عامّ: نصوصٌ محايدة بلا أعدادِ فروعٍ ولا مساراتٍ محلّيّة. **لا يُغلق بندٌ بلا قياس:** ما لم يُتحقَّق يُذكر في ملاحظته.
"""

import datetime

from django.db import migrations

STAMP = "[2026-09-26]"
DAY = datetime.date(2026, 9, 25)  # يومُ القياس لا يومُ التسجيل
D = datetime.date


# (الرمز، (الحالة، التقدّم) المتوقَّعان، الحالة الجديدة، التقدّم الجديد، رفعُ البوّابة، رمزُ الطلب يُضاف، سطر الملاحظة)
UPDATES = [
    (
        "Q-06",
        ("todo", 0),
        "done",
        100,
        True,
        "#643",
        "**Q-06 منجز (#643 اندمج؛ غيرُ منشورٍ عند الكتابة):** مراجعةُ WCAG 2.2 للجوال بـChromium (محاكاةُ 375 و320 و1280 على عشر صفحات، بحسابٍ إداريٍّ، "
        "«بصريّ · محور الجوال»، 2026-09-25). **المقيس:** 2.4.11 — العنصرُ المركَّز المحجوبُ تحت الترويسة والشريط السفليّ كان محجوباً كلّيّاً في 65 من 66 صفّاً في سجلّ "
        "الطلاب وفي 60 كلّيّاً و46 جزئيّاً من 106 في الإشعارات، فصار 0؛ 1.4.10 — فيضٌ أفقيٌّ 0 عند 320 في الصفحات العشر؛ 1.4.12 — لا قصَّ جديداً عند فرض التباعد "
        "(وأُزيل قصُّ نصّ الإشعار وهو بند N-045)؛ 2.5.7 — ثلاثةُ مواضعَ كلٌّ ببديلٍ بنقرة؛ 3.3.8 — رمزُ التحقّق يقبل الملصوق «123 456»؛ 4.1.3 — الإشعارُ العائم "
        "`alert` للخطأ والتحذير و`status` لما سواهما. **الحارس:** tests/test_wcag22_mobile.py (16 اختباراً) والوثيقةُ docs/wcag22_mobile_review_2026-09.md؛ وصافي CSS "
        "الخامّ −145 بايتاً؛ بلا هجرة. **لم يُقَس ولا يُحسب مطابقةً:** جهازٌ حقيقيّ، وWebKit، وقارئُ الشاشة (Q-09)، والعمودُ اللاصقُ الأفقيّ في السجلّات العريضة. "
        "**فالخمسةُ من خمسة (MK23) بـChromium وحدَه.** معاينةُ المالك المطلوبة: تمريرُ التركيز بلوحة المفاتيح في الصفحات الطويلة وحقلُ رمز التحقّق.",
    ),
    (
        "DBT-46",
        ("todo", 0),
        "done",
        100,
        True,
        "#645",
        "**DBT-46 منجز (#645 اندمج؛ غيرُ منشورٍ عند الكتابة):** عناوينُ أعمدة سجلّ الموظفين تسع أطولَ كلمةٍ فيها. **المقيس على الصفحة نفسها قبل/بعد («بصريّ · تخطيط لوحة "
        "المدير»، 2026-09-25):** العناوينُ الأضيقُ من كلمتها 9/9/8/6/5 ← 0 عند 641/700/768/820/900px؛ وفيضُ المستند الأفقيّ 0 على 13 عرضاً من 375 إلى 1920؛ وصافي CSS "
        "الخامّ −387 بايتاً؛ وحارسٌ حيٌّ في e2e (`test_staff_register_headers_live`) يفشل على CSS الأمس. **لم يُتحقَّق:** معاينةُ المالك للصفحة.",
    ),
    (
        "REP-08",
        ("doing", 0),
        "doing",
        60,
        False,
        "",
        "**التقدّم 60% — اقتراحُ مسار الديون التقنية (صاحب المسار) لا قياس:** الجولةُ الأولى من الحذف نُفِّذت بيد المالك بإذنه الصريح على مانيفستٍ محدَّثٍ وبعد نسخةٍ "
        "احتياطيّةٍ سليمة (حزمةُ git ومجموعُ تحقّق)، وحُفظت الفروعُ الفريدةُ بوسومٍ محلّيّةٍ لا تُدفع، والاسترجاعُ من أوامرَ محفوظة. **ما بقي:** جولةٌ ثانيةٌ للفروع الموسومة، "
        "وفروعٌ ضمن مهلة الـ48 ساعة تُستنزف بعد انقضائها مساءَ الأحد 09-27 فصاعداً — بدفعةٍ واحدةٍ يشغّلها المالكُ بعد إعادة الأدلّة بدقائق (الأداةُ الجديدةُ تعيدها كلَّ مرّة). "
        "**لا قراءةَ رسميّةً لـRK1 بعدُ:** تُؤخذ بأداة `prune_local_branches.py --json` بعد أن تهدأ الحذوفاتُ (سُحبت قراءتان سبقتا التنفيذ).",
    ),
    (
        "REP-09",
        ("todo", 0),
        "doing",
        85,
        False,
        "",
        "**نُفِّذ الجزءُ الأكبر بيد المالك بإذنه (2026-09-25 ~22:05 الدوحة):** حُذفت عشرُ قواعدِ اختبارٍ يتيمةٍ (≈199MB) من قاعدة postgres المحلّيّة المشتركة بعد فحصٍ حيٍّ أنّها "
        "بلا اتّصالٍ ولا شجرةٍ ولا جلسة، وتحقّقٌ بالقراءة أنّها غائبة؛ ولا حاويةَ يتيمةً ولا شبكةً ولا مجلّداً فارغاً ولا شجرةً متداخلة. **RK3 (أشجارٌ راكدة) = 0 مرشّحة يومَ التنفيذ** "
        "(الشجرتان المرشَّحتان أُعيد استعمالُهما لجلستين حيّتين فأُخرجتا) — **درسٌ للمانيفستات: تُعاد الأدلّةُ قبل كلّ حذف لأنّ أسماء الأشجار تُعاد لجلساتٍ جديدة في ساعات.** "
        "**ما بقي:** اختصارُ نسخ `.env` الزائدة إلى مرجعٍ واحد — قرارٌ يلزمه رأيُ أمين المستودع في `scripts/session-db.sh` (لا حذفَ مقترَح) — والصيانةُ الدوريّة. "
        "**التقدّم 85% اقتراحُ صاحب المسار لا قياس.**",
    ),
    (
        "REP-10",
        ("doing", 70),
        "doing",
        75,
        False,
        "#640 #644",
        "**تُوحَّد النسبةُ على رقم مالك مسار البند (مسار الجودة): 75%** بعد أن كانت 0035 كتبت 70% اقتراحَ مقدِّم الطلب. **#640 اندمج:** بطاقةُ «صحّة Git» في رئيسيّة الإدارة "
        "(آخرُ قراءةٍ لـRK1..RK3 وعمرُها)، و**#644 اندمج:** اختبارٌ مسمّىً لحالة دمجٍ مسحوقٍ بناتجٍ معدَّل ⇒ REVIEW لا آمن. **التقدّم 75% = ثلاثٌ من أربع (السكربتُ والاختبارُ "
        "والبطاقةُ ✓؛ الجدولةُ الأسبوعيّةُ وأوّلُ قراءةٍ مسجَّلةٍ ✗ — مسجَّلةٌ للأحد 06:30 ولم تعمل بعدُ) — اقتراحُ صاحبه لا قياس.** ولا قراءةَ رسميّةً لـRK1 بعدُ (انظر REP-08).",
    ),
    (
        "REP-11",
        ("todo", 0),
        "doing",
        0,
        False,
        "#650",
        "**بدأ التنفيذُ ولا نسبةَ مقترحة (لم يقترحها صاحبُ البند):** الجزءُ المحلّيّ من إعداد git المشترك بين الأشجار مُنفَّذٌ بإبلاغ الحوكمة عن أمين المستودع — خطّافُ pre-commit مشتركٌ "
        "لكلّ الأشجار (أوّلُ إيداعٍ قد يفشل مرّةً فيُعاد `git add` ثمّ الإيداع)، وبريدُ الإيداع noreply، وفصلُ upstream، و`autoSetupMerge=simple`. **#650 اندمج:** إعداداتُ Claude "
        "المحلّيّة خارج التتبّع ومتجاهَلةٌ في `.gitignore`. **لم يُتحقَّق:** ما بقي من بنود REP-11 غيرُ المبلَّغ إنجازُه.",
    ),
    (
        "N-047",
        ("doing", 50),
        "doing",
        50,
        False,
        "#657",
        "**#657 اندمج (28cab198؛ غيرُ منشورٍ عند الكتابة) بقرار المالك المباشر:** رؤيةُ الوزارة تبقى في السطر تحت الجدول وتُحذف من ذيل مصفوفة الورقة (matrix-foot) وحدَه — فلا تظهر "
        "مرّتين (كان تنبيهُ هذا البند). **المقيس («جدول · التشغيل»، 2026-09-25):** PDF قبل/بعد على A3 وA4 (خطّة، وأسبوعٌ فعليٌّ بتبديلٍ وتعويض، وأسبوعُ إجازة): عددُ الصفحات كما كان "
        "(A3 صفحةٌ واحدة، A4 صفحتان)؛ والفرقُ مربّعٌ واحدٌ نحو 113×13px هو نصُّ الرؤية في ذيل الصفحة الأخيرة؛ وأسبوعُ الإجازة مطابقٌ بكسلاً؛ و37 اختباراً ناجحاً (منها جديدٌ: الرؤيةُ "
        "تُذكر مرّةً واحدةً في السطر لا في الذيل). **لم يُتحقَّق:** معاينةُ المالك على الإنتاج؛ والتقدّمُ باقٍ 50% ولا يُغلق قبل معاينته.",
    ),
    (
        "N-048",
        ("doing", 0),
        "doing",
        50,
        False,
        "#652",
        "**#652 اندمج (bb8b56a0؛ غيرُ منشورٍ عند الكتابة):** «الجدول العام» تحت «الجدولة الذكيّة» في «إدارة الشؤون الأكاديميّة» بطلب المالك، بلا CSS. **لم يصل بعدُ قياسُ المنفِّذ** "
        "(يُبلَّغ عند النشر)؛ **لم يُتحقَّق:** ظهورُ الرابط في القائمة على الإنتاج، وأنّه لا يتبدّل من يرى بقيّة البنود. **التقدّم 50% = خطوتان من أربع (التنفيذُ والدمجُ ✓؛ النشرُ "
        "ومعاينةُ المالك ✗) — اشتقاقٌ لا قياس.**",
    ),
    (
        "LAY-03",
        ("doing", 50),
        "doing",
        50,
        False,
        "#651",
        "**#651 اندمج (5dc4acda؛ غيرُ منشورٍ عند الكتابة) — لوحةُ القيادة تسع 1366×768 بلا تمرير («بصريّ · تخطيط لوحة المدير»، 2026-09-25):** بلاطاتُ الانتقال سطرٌ واحدٌ والرسمُ يتبع "
        "النافذة. **المقيس كما ورد في تقرير صاحب الطلب (وحدةُ الأوّل لم تُدوَّن):** التجاوزُ عند 1366×768 للوحتَي المدير والمطوّر 126 ← 0؛ وعلى 163 صفحةً انخفض عددُ الصفحات "
        "الممرَّرة 51 ← 43 بلا صفحةٍ أسوأ؛ وصافي CSS الخامّ −1 بايت. لم تُغيَّر حالةُ البند ولا تقدّمُه ولا موعدُه.",
    ),
    (
        "LAY-10",
        ("todo", 0),
        "doing",
        50,
        False,
        "",
        "**#656 مفتوحٌ لم يندمج** (قرارُ المالك 2026-09-25 — فصلُ الشاشة عن ورقة الطباعة؛ «جدول · التشغيل»)، وبطلب المالك **لا يُنشر قبل استقرار الأحد 09-27**؛ يُسجَّل «مفتوحٌ ينتظر دمجَ جلسة "
        "النشر». الجدولُ العامّ جدولٌ أصيلٌ بمكوّنات المنصّة والطباعةُ كما هي. **المقيس محلّيّاً (2026-09-25):** PDF قبل/بعد مطابقٌ بكسلاً في 8 صفحات (A3/A4 × خطّة/أسبوعٌ فعليٌّ "
        "بتبديل/إجازة) وHTML الورقة مطابقٌ بعد حذف ما أُزيل عمداً وحدَه؛ خاناتُ الشاشة = خاناتُ الورقة (اختبارٌ للخطّة ولأسبوعٍ محوَّل)؛ لا postMessage بين وثيقتين (اختبارٌ يمنعه)؛ "
        "وحسابُ المصفوفة عند فتح الصفحة كان 18–23 استعلاماً ونحو 0.5ث (**تقريبيّة**) وصار الإطارُ يُحمَّل عند «طباعة» فقط؛ ومتصفّحٌ حقيقيّ عند 1920/1366/1024/390 نهاراً وليلاً: التفاعلُ يعمل "
        "والطباعةُ تحمّل الإطارَ عند النقرة. **صافي CSS الخامّ +9.9KB** والهامشُ تحت 470KB بعده 24.6KB — **سؤالٌ لمالك مسار الواجهة: أيُقبل؟** (VD8: لا رفعَ للسقف). **لم يُقَس:** رؤيةُ المالك على "
        "الإنتاج (شاشةٌ نهاراً وليلاً، وهاتفٌ حقيقيّ، وزرُّ «طباعة» بنافذةٍ حقيقيّة) ووحداتُ الويب. **التقدّم 50% = التصميمُ والتنفيذُ والقياسُ المحلّيّ ✓؛ الدمجُ والنشرُ ومعاينةُ المالك ✗ "
        "— اشتقاقٌ لا قياس.**",
    ),
]

# ملاحظاتٌ على بنودٍ قائمة تُلحق مرّةً واحدة (بلا لمس الحالة ولا التقدّم)
NOTES = [
    (
        "VI-13",
        "**#646 اندمج (dc070f37) — إصلاحُ أساس main:** دمجان متتاليان على main لا يُلغيان بناءَ أساس اللقطات. **فائدةٌ لـV-K25 (المقيس في CI، «بصريّ · تخطيط لوحة المدير»):** أوّلُ إثباتٍ "
        "طرفٍ لطرف أنّ وظيفةَ اللقطات تلتقط تغييراً بصريّاً متعمَّداً (طلبُ #651: لوحةُ القيادة 8.848% نهاراً و9.317% ليلاً فوق العتبة) وأنّ حتميّتَها 0.000%. "
        "**V-K25 (0 ← 5) لا يُحتسب** قبل أن يبني main أساساً ويقارن به طلبٌ فعلاً.",
    ),
    (
        "OWN-30",
        "**تحقّقُ مسار الديون على main@0df2d5bf (2026-09-25): مستوفٍ بنيويّاً** — `requirements-mypy.txt` مصدرٌ واحدٌ (mypy==1.10.0 وdjango-stubs==5.0.2) و`requirements-dev.txt` "
        "يستدعيه بـ`-r`، ووظيفةُ mypy في بوّابة الجودة تثبّت منه، وdependabot يستثني mypy وdjango-stubs وdjangorestframework-stubs، وحارسُ tests/test_mypy_pins_are_one_source.py قائمٌ، "
        "وأساسُ السقّاطة لم يتغيّر. **لم يُتحقَّق:** معيارُ «`python -m tests.mypy_ratchet` في حاوية التطوير يعطي الأساسَ المسجَّل» (حاويةُ التطوير على الإصدار القديم حتى تُعاد بناؤها — "
        "مرتبطٌ بـDBT-16)، ولا خُضرةُ وظيفة mypy في آخر تشغيلٍ على main (التشغيلاتُ ملغاةٌ أو جاريةٌ) — تُقرأ صباحَ الاثنين.",
    ),
    (
        "DBT-01",
        "**D-26 حُسم (2026-09-25، المالكُ لجلسة الخارطة مباشرةً): تُخرَج أوامرُ الإدارة من نطاق mypy.** أُسند البندُ لأمين المستودع (يومٌ واحدٌ، غيرُ عاجل، قبل 10-16)؛ استلمه "
        "ولن يزاحم موعدَ 10-02، ويبدأه بعد REP-11 وREP-19 ويبلغ برقم الطلب وقياس PK6 بأمرٍ يُعاد إنتاجُه. **قياسُ أمين المستودع (2026-09-26؛ الطلب #660 مفتوحٌ لم يندمج): الأساسُ بعد الإخراج "
        "1,329 خطأً في 123 ملفّاً — لا 1,396 في 126 كما قدّر مسارُ الديون** (من 1,735 في 160، وقد أُعيد إنتاجُها قبل الاستثناء فطابقت المسجَّلَ؛ خرج 37 ملفّاً). "
        "**أثرُ النطاق:** ثلاثُ وحداتٍ (assessments/services وstudent_affairs/services وscripts/seed_data، 67 خطأً) لا يبلغها mypy إلّا باستيراد الأوامر فصارت غيرَ مفحوصةٍ "
        "ما لم تستوردها شيفرةٌ مفحوصة. والقياسُ في بيئةٍ تطابق وظيفةَ CI (لا حاويةُ التطوير: تعطي رقماً آخر). **انخفاضُ PK6 هنا «تغييرُ نطاق» لا تقدّم** "
        "ولا يُحسب إغلاقاً للبند؛ **ولا يُسجَّل PK6 قبل اندماج #660.**",
    ),
]

# تصحيحُ عبارةٍ كتبتها الخارطةُ نفسُها: (الرمز، النصُّ القديم بالضبط، النصُّ الجديد، سطرُ التصحيح)
CORRECTIONS = [
    (
        "N-046",
        "**نقلٌ لم يؤكّده المالكُ لجلسة الخارطة مباشرةً**",
        "**أكّده المالكُ لجلسة الخارطة مباشرةً (2026-09-25 مساءً)**",
        "**تصحيحٌ لعبارة 0034:** كانت تقول إنّ قرارَ «الخادمُ المقيمُ الوحيد» نقلٌ لم يؤكّده المالكُ لجلسة الخارطة مباشرةً؛ وقد أكّده لها مباشرةً بعد ذلك — يتبع main، وبياناتُه "
        "مزروعةٌ غيرُ شخصيّة، ويُثبَّت مؤقّتاً على شجرة جلسة. التقدّمُ لم يتغيّر.",
    ),
]

# بنودٌ جديدة: (الرمز، المصدر، المسار، العنوان، الاعتماديّات، المعيار، الحالة، التقدّم، البداية، النهاية، أساسُ التاريخ، الجهد، البوّابة، طلب الدمج، المرجع، الملاحظة، الترتيب)
NEW_ITEMS = [
    (
        "DBT-57",
        "DBT",
        "debt",
        "سقّاطةُ قمع المُسكِتات: عدُّ `noqa` و`type: ignore` لا يزيد (أساسٌ لكلّ ملفّ)",
        "DBT-01",
        "أداةٌ `tests/suppressions_ratchet.py` تعدّ المُسكِتات بـtokenize (لا بتعبيرٍ نمطيّ) وتقارن بأساسٍ لكلّ ملفّ فتسقط على أيّ زيادةٍ وتسمح بالنقص؛ وتُضاف إلى خريطة الحرّاس "
        "في الطلب نفسه.",
        "todo",
        0,
        D(2026, 10, 9),
        D(2026, 10, 9),
        "موعدٌ مقترَحٌ أكّده المالكُ لجلسة الخارطة 2026-09-25",
        1.0,
        "",
        "",
        "DBT-01 · D-26",
        "**بندٌ جديدٌ بتأكيد المالك المباشر (2026-09-25)** — اقتراحُ مسار الديون التقنية: بعد إخراج أوامر الإدارة من نطاق mypy (D-26) لا يمنع شيءٌ إسكاتَ المدقّق بتعليقٍ؛ وقد ازداد "
        "عددُ `noqa` و`type: ignore` من 158 إلى 168 في يومٍ واحد (**اقتراحٌ لا قياسٌ لي**). يتبع DBT-01 والمقعدُ أمينُ المستودع. **الجهدُ غيرُ مقدَّر (القيمةُ الافتراضيّة).** "
        "لم يُنفَّذ منه شيء.",
        753,
    ),
    (
        "N-049",
        "NEW",
        "product",
        "تصحيحُ سلّم المخدّرات والأسلحة (4-14/4-15) في دليل السلوك 2026 على ص125: صفُّ «إحالة الطالب إلى الجهة المختصّة» أوّلُ صفوف «الثانية»، واسمُ 4-07 كما في ص117، "
        "ومقدِّرُ الضرر في 4-09/4-10 «إدارة المدرسة» (ص120)",
        "",
        "الكودُ (`behavior/conduct_2026.py`) يطابق صفحاتِ الدليل الثلاث بتحقّقٍ بصريّ؛ وهجرةُ بياناتٍ تُصحّح اسمَ 4-07 في القاعدة (نسخةُ الكتالوج نُسخت مرّةً بهجرة سابقة)؛ "
        "وأربعةُ اختباراتٍ تسقط على الكود القديم.",
        "done",
        100,
        D(2026, 9, 26),
        D(2026, 9, 26),
        "اندمج #658 بأمر المالك 2026-09-26",
        1.0,
        "",
        "#658",
        "دليل السلوك 2026 ص117 وص120 وص125",
        "**#658 اندمج (4bba234c، 2026-09-26؛ غيرُ منشورٍ عند الكتابة) بطلب مباشرٍ من المالك؛ لم يكن للعمل بندٌ فعُيِّن N-049** (قاعدةُ الإدراج: لا بندَ لتدقيق نسخة الوزارة مقابل "
        "الـPDF ولا لسلالم دليل السلوك). مصدرُ الاكتشاف: تدقيقُ جلسة «مستشار ومصدر الحقيقة للمنصّة» (2026-09-25)؛ وتحقّقت الجلسةُ المنفِّذةُ من 4-13 (ص124) فوجدت الكودَ فيه صحيحاً. "
        "**المقيس (الجلسة المنفِّذة، 2026-09-26):** 356 اختباراً نجح في خادم الجلسة (فشل اثنان بيئيّان: غيابُ git في الحاوية) و23 فحصاً أخضر في الطلب، وخلا `makemigrations --check` "
        "من أيّ تغيير. **لم يُتحقَّق:** تطبيقُ هجرة البيانات behavior/0020 وظهورُ الاسم الجديد على الإنتاج (النشرُ عند جلسة النشر). "
        "**ما بقي — خارج هذا البند ومحجوبٌ بقرار المالك لا منجَز:** تصحيحُ ملفّات md المرجعيّة (اسمُ 4-07 القديم، وترتيبُ سلّم 4-15 وترتيبُ 4-09/4-10) عند المستشار؛ ولا بندَ له قبل قرار المالك.",
        754,
    ),
]

# قرارٌ محسوم بتأكيد المالك المباشر: (الرمز، المصدر، القرار، يحجب، الخيارات، التوصية، تاريخ الحسم، الترتيب)
NEW_DECISIONS = [
    (
        "D-26",
        "DBT",
        "نطاقُ mypy: تُخرَج أوامرُ الإدارة (management commands) من سقّاطة الأخطاء المسجَّلة",
        "DBT-01 (وقراءةُ PK6 بعده)",
        "(أ) تُخرَج أوامرُ الإدارة من النطاق فيُعاد تسجيلُ الأساس بعدها. (ب) يبقى النطاقُ كما هو.",
        "(أ) — حسمه المالكُ لجلسة الخارطة مباشرةً (2026-09-25). **انخفاضُ PK6 بعده تغييرُ نطاقٍ لا تقدّم.** الأساسُ الجديدُ يُسجَّل "
        "بقياسٍ في بيئة CI لا بتقدير (انظر DBT-01).",
        D(2026, 9, 25),
        140,
    ),
]

# قراءةُ مؤشّرٍ لم يُمسّ منذ آخر لقطة: (الرمز، (القيمة، تاريخ القياس) المتوقَّعان، القيمة الجديدة، مرجعُ القياس يُلحق بالمصدر)
KPI_UPDATES = [
    (
        "MK23",
        (None, None),
        5.0,
        "#643، 09-25: 5 من 5 بـChromium وحدَه (لا جهازٌ حقيقيّ ولا WebKit ولا قارئُ شاشة)",
    ),
    ("PK28", (0.0, None), 5.0, "مرآة MK23 (#643، 09-25): بـChromium وحدَه"),
    (
        "RK3",
        (8.0, DAY),
        0.0,
        "مسار الديون بعد REP-09، 09-25: 0 مرشّحة (أُعيد استعمالُ شجرتَين لجلستَين حيّتين)",
    ),
]


def _add_pr(existing, token):
    """تُضيف رمزَ الطلب دون تكرارٍ ودون تجاوز حدّ الحقل 64 (وإلّا يبقى الحقلُ كما هو)."""
    if not token:
        return existing
    joined = " ".join(dict.fromkeys([*existing.split(), *token.split()]))
    return joined if len(joined) <= 64 else existing


def sync(item_model):
    """تُطبِّق تحديثاتِ البنود الحارسة؛ تُرجع رموزَ ما تغيّر."""
    changed = []
    for code, expected, status, progress, clear_gate, pr, line in UPDATES:
        item = item_model.objects.filter(code=code).first()
        if item is None or (item.status, item.progress) != expected or line in item.note:
            continue
        item.status, item.progress = status, progress
        item.pr = _add_pr(item.pr, pr)
        if clear_gate:
            item.gate = ""
        item.note = f"{item.note}\n{STAMP} {line}".strip()
        item.save(update_fields=["status", "progress", "pr", "gate", "note", "updated_at"])
        changed.append(code)
    return changed


def sync_notes(item_model):
    """تُلحق الملاحظاتِ بالبنود القائمة مرّةً واحدة؛ تُرجع رموزَ ما تغيّر."""
    changed = []
    for code, line in NOTES:
        item = item_model.objects.filter(code=code).first()
        if item is None or line in item.note:
            continue
        item.note = f"{item.note}\n{STAMP} {line}".strip()
        item.save(update_fields=["note", "updated_at"])
        changed.append(code)
    return changed


def correct_notes(item_model):
    """تُصحِّح عبارةً كتبتها الخارطةُ نفسُها ما دامت موجودةً بنصّها، وتُلحق سطرَ التصحيح؛ تُرجع رموزَ ما تغيّر."""
    changed = []
    for code, old, new, line in CORRECTIONS:
        item = item_model.objects.filter(code=code).first()
        if item is None or old not in item.note or line in item.note:
            continue
        item.note = f"{item.note.replace(old, new)}\n{STAMP} {line}".strip()
        item.save(update_fields=["note", "updated_at"])
        changed.append(code)
    return changed


def add_new_items(item_model):
    """تُنشئ البنودَ الغائبة؛ لا تُعيد كتابةَ بندٍ موجود؛ تُرجع رموزَ ما أُنشئ."""
    created = []
    for (
        code,
        src,
        lane,
        title,
        deps,
        criterion,
        status,
        progress,
        start,
        end,
        basis,
        effort,
        gate,
        pr,
        ref,
        note,
        order,
    ) in NEW_ITEMS:
        if item_model.objects.filter(code=code).exists():
            continue
        item_model.objects.create(
            code=code,
            src=src,
            lane=lane,
            title=title,
            status=status,
            progress=progress,
            start_date=start,
            end_date=end,
            date_basis=basis,
            effort=effort,
            deps=deps,
            criterion=criterion,
            gate=gate,
            pr=pr,
            ref=ref,
            note=f"{STAMP} {note}",
            sort_order=order,
        )
        created.append(code)
    return created


def add_decisions(decision_model):
    """تُنشئ القراراتِ المحسومةَ الغائبة بتأكيد المالك؛ لا تُعيد كتابةَ قرارٍ موجود؛ تُرجع رموزَ ما أُنشئ."""
    created = []
    for code, src, title, blocks, options, recommendation, decided_on, order in NEW_DECISIONS:
        if decision_model.objects.filter(code=code).exists():
            continue
        decision_model.objects.create(
            code=code,
            src=src,
            title=title,
            status="decided",
            decider="المالك",
            due="",
            decision_date=decided_on,
            blocks=blocks,
            options=options,
            recommendation=f"{STAMP} {recommendation}",
            sort_order=order,
        )
        created.append(code)
    return created


def _record(kpi, value):
    """تكتب قياسَ يومه في السجلّ: تستبدل نقطةَ اليوم إن وُجدت، وإلّا تُلحقها."""
    today = DAY.isoformat()
    history = [point for point in (kpi.history or []) if point.get("d") != today]
    history.append({"d": today, "v": value})
    kpi.current, kpi.measured_at, kpi.history = value, DAY, history


def _with_source(kpi, note):
    """تُلحق مرجعَ القياس بالمصدر دون تكرارٍ وبحدّ الحقل 255 (وإلّا يبقى المصدرُ كما هو)."""
    if not note or note in kpi.source:
        return kpi.source
    source = f"{kpi.source} ({note})".strip()
    return source if len(source) <= 255 else kpi.source


def sync_kpis(kpi_model):
    """تُحدِّث المؤشّراتِ التي لم تُمسّ منذ آخر لقطة؛ تُرجع رموزَ ما تغيّر."""
    changed = []
    for code, expected, value, note in KPI_UPDATES:
        kpi = kpi_model.objects.filter(code=code).first()
        if kpi is None or (kpi.current, kpi.measured_at) != expected:
            continue
        source = _with_source(kpi, note)
        _record(kpi, value)
        kpi.source, kpi.why = source, ""
        kpi.save(update_fields=["current", "measured_at", "history", "source", "why", "updated_at"])
        changed.append(code)
    return changed


def forwards(apps, schema_editor):
    item_model = apps.get_model("roadmap", "RoadmapItem")
    # قاعدةٌ بلا استيراد (اختبار، شجرةٌ جديدة): لا شيء يُزامَن، ولا بنودٌ يتيمة.
    if not item_model.objects.exists():
        return
    sync(item_model)
    sync_notes(item_model)
    correct_notes(item_model)
    add_new_items(item_model)
    add_decisions(apps.get_model("roadmap", "RoadmapDecision"))
    sync_kpis(apps.get_model("roadmap", "RoadmapKpi"))


class Migration(migrations.Migration):
    dependencies = [
        ("roadmap", "0035_families_and_published_2026_09_25"),
    ]

    operations = [
        migrations.RunPython(forwards, migrations.RunPython.noop),
    ]
