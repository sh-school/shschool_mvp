"""مزامنةُ الخارطة العشرون (2026-09-25 مساءً): ما نُشر على main@4e27672 (نحو 19:58 الدوحة، بشهادة جلسة النشر)، وبندُ إصلاح الإشعار بطلب المالك.

حارسةٌ كسابقاتها: لا يُلمس بندٌ إلّا إن كانت حالتُه وتقدّمُه ما في آخر لقطة، ولا تُكرَّر ملاحظةٌ، ورمزُ الطلب يُضاف بحدّ الحقل 64، ويُنشأ الغائبُ وحدَه.
والمستودعُ عامّ: نصوصٌ محايدةٌ بلا أسماءٍ ولا أعدادٍ حسّاسة. **لا يُغلق بندٌ بلا قياس:** ما لم يُتحقَّق يُذكر في ملاحظته.

- **OWN-30 → منجز** (#624): توحيدُ إصدار فاحص الأنواع بين التطوير وCI.
- **OWN-21:** تعميقُ «مسحِ الإدارة كلِّها» (#615) — ملاحظةٌ ورمزُ طلبٍ؛ البندُ منجزٌ أصلاً، وتبقى معاينةُ المالك.
- **N-042 → 83%** (#626): نُشر ونجح تحقّقُ العامل وقُرئ Sentry بعد النشر؛ ولا يُغلق قبل تجربة المالك تصديرَ PDF حقيقيّاً.
- **REP-07:** ملاحظةُ REP-07b (#623) — الأسماءُ الكاملةُ للموظّفين خارج الشيفرة؛ لا يُعلن إغلاقُه قبل خضرة CI على main.
- **N-045 جديد** (#628 — بلاغُ المالك، بطلبه): نصُّ الإشعار يُقرأ كاملاً في صندوق الإشعارات؛ مسارُ الواجهة والهويّة، قيد التنفيذ 50% اشتقاقاً.
"""

import datetime

from django.db import migrations

STAMP = "[2026-09-25]"
D = datetime.date


# (الرمز، (الحالة، التقدّم) المتوقَّعان، الحالة الجديدة، التقدّم الجديد، رفعُ البوّابة، رمزُ الطلب يُضاف، سطر الملاحظة)
UPDATES = [
    (
        "OWN-30",
        ("todo", 0),
        "done",
        100,
        True,
        "#624",
        "**OWN-30 منجز ومنشور (#624، اندمج 19:40 الدوحة ونُشر ضمن main@4e27672 نحو 19:58 بشهادة جلسة النشر):** ملفٌّ واحدٌ requirements-mypy.txt يقرؤه "
        "التطويرُ وCI، واستثناءُ dependabot لإصدارَي الفاحص، و`--update` يرفض تسجيلَ خطّ الأساس بغير الإصدار الثابت. **المقيس (8097، 2026-09-25):** السقّاطةُ "
        "بالإصدارات الثابتة طابقت المسجَّلَ (1,735 خطأً في 160 ملفّاً) بلا رفعٍ لخطّ الأساس. **لم يُتحقَّق:** خضرةُ بوّابة الجودة على main بعد الدمج (لم تكتمل "
        "عند الكتابة؛ أُلغي تشغيلٌ سبقها بدفعٍ أحدث). وموعدُه (10-02) لم يُعدَّل — أُنجز قبله.",
    ),
    (
        "OWN-21",
        ("done", 100),
        "done",
        100,
        False,
        "#615",
        "**تعميقٌ: #615 «مسحُ الإدارة كلِّها» اندمج ونُشر (ضمن main@4e27672 نحو 19:58 الدوحة؛ بلا هجرة):** كلُّ ModelAdmin مسجَّلٍ (قائمتُه وصفحةُ إضافته) "
        "والفهرسُ = 382 صفحةً على 375px نهاراً وليلاً. **المقيس (8096، 2026-09-25، بأداة tests/e2e/test_admin_sweep.py — تُشغَّل بـADMIN_SWEEP=1 لا في CI "
        "العاديّ لأنّها نحو 25 دقيقة — على شجرةٍ محلّيّةٍ بالبيانات نفسها لا الإنتاج):** قبل الإصلاح 116 هدفَ لمسٍ دون 44px و26 عقدةَ axe (تباينٌ وأسماء)؛ بعده "
        "صفرٌ في الاثنين وصفرُ تجاوزٍ أفقيّ. والحرّاسُ الدائمةُ في CI (وظيفةُ axe-a11y): زيدت صفحاتُ tests/test_a11y_axe_ratchet.py (axe ولمس). "
        "**منجَزٌ بالقياس المحلّيّ، ينتظر معاينةَ المالك البصريّةَ على الإنتاج:** /admin/ على الجوّال بعرض 375 (البطاقاتُ العشر، وشريطُ الحفظ اللاصق، وأزرارُ "
        "صفحات الأخطاء)؛ ومسحُ Playwright الشاملُ نفسُه لا يجري على الإنتاج.",
    ),
    (
        "N-042",
        ("doing", 0),
        "doing",
        83,
        False,
        "#626",
        "**#626 اندمج (24d9273b، 19:37 الدوحة) ونُشر ضمن main@c5c0857 نحو 20:13 بشهادة جلسة النشر — لم يُغلق البندُ بعد:** أقلع العامل بـcollectstatic "
        "(سجلُّ الإقلاع: 270 ملفّاً ساكناً نُسخت و1232 عُولجت ثمّ celery ready)؛ و`/app/staticfiles/staticfiles.json` موجودٌ في حاوية العامل (270 مدخلاً، فيه "
        "js/actions.js)؛ وتصييرُ print_schedule.html داخل العامل ناجحٌ بوضعَي الشاشة وPDF؛ ولا حدثَ «Missing staticfiles manifest» في سجلّ العامل منذ إعادة "
        "التشغيل. حاويةُ celery-beat بلا manifest (لا تصيّر قوالب؛ خارجَ نطاق الإصلاح). **قُرئ Sentry بعد النشر (منصّة · فحص الإدارة، 2026-09-25):** المشكلةُ المسجَّلة (ثلاثةُ أحداث) ما زالت «unresolved» شكلاً لكنّ آخرَ حدثٍ لها قبل النشر بنحو ست ساعات وعددَها لم يزد — نافذةٌ قصيرةٌ (نحو ربع ساعة) لم يجرِ فيها تصديرٌ حقيقيّ، فلا تُحسم بها وحدَها. **لم يُتحقَّق بعد (وهو من معيار البند):** تصديرُ schedule.pdf A3 حقيقيّاً من الواجهة (عند المالك) وألّا تتجدّد المشكلةُ بعده. **التقدّم 83% = خمسُ خطواتٍ من ستّ (الإصلاحُ والاختبارُ والنشرُ وتحقّقُ العامل وقراءةُ Sentry ✓؛ تجربةُ المالك ✗) — اشتقاقٌ لا قياس.**",
    ),
]

# بنودٌ جديدة: (الرمز، المصدر، المسار، العنوان، الاعتماديّات، المعيار، الحالة، التقدّم، البداية، النهاية، أساسُ التاريخ، الجهد، البوّابة، طلب الدمج، المرجع، الملاحظة، الترتيب)
NEW_ITEMS = [
    (
        "N-045",
        "NEW",
        "frontend",
        "الإشعارات: نصُّ الإشعار يُقرأ كاملاً في صندوق الإشعارات (بلاغُ المالك: مقطوعٌ ولا يُقرأ، والنقرُ يفتح صفحةً أخرى)",
        "",
        "نصٌّ أطولُ من 160 حرفاً يظهر كاملاً في صفّ الإشعار (لا قصَّ بالحرف ولا بالأسطر) وسطرُه الجديد محفوظ؛ وحارسٌ يمنع عودةَ القصّ؛ وتحقّقُ المالك "
        "بصريّاً على الحاسوب وعلى جهاز لمسٍ حقيقيّ بعد النشر.",
        "doing",
        50,
        D(2026, 9, 25),
        D(2026, 9, 26),
        "بلاغُ المالك 2026-09-25؛ النشرُ بعد الدوام",
        0.5,
        "",
        "#628",
        "مسار الواجهة والهويّة 8205 — بلاغُ المالك",
        "**بلاغُ المالك (2026-09-25):** نصُّ إشعارٍ مقطوعٌ بـ«…» ولا سبيلَ لقراءته كاملاً، والنقرُ على عنوانه يفتح صفحةً أخرى. **السبب (فُحص على القالب والأنماط):** "
        "القالبُ يطبع النصَّ بعد `truncatechars:160` فيُحذف ما بعده من الصفحة، والأنماطُ تقصّ الفقرةَ بسطرين، والنصُّ الكاملُ في `title` (تلميحٌ بالمرور لا يعمل "
        "على اللمس)؛ وليس للإشعار صفحةُ تفصيل — الرابطُ الوحيدُ عنوانُه إلى `related_url`، وعلى اللمس صار الصفُّ كلُّه رابطاً منذ M-02 (#616). "
        "**الإصلاح (#628):** النصُّ كاملاً بلا قصٍّ وسطرُه الجديد محفوظ + حارسٌ يمنع العودة. **المقيس (أمين خارطة التجويد، 2026-09-25):** 43 اختباراً ذا صلة تمرّ "
        "(بينها حارسُ M-02)؛ وعيّنةٌ مصيَّرةٌ نصُّها 259 حرفاً: قبل الإصلاح تنتهي بـ«…» وبعده تظهر كاملةً (لقطةٌ قبل/بعد). **لم يُتحقَّق:** على الإنتاج ولا على جهاز لمسٍ حقيقيّ "
        "(عند المالك). **التقدّم 50% = ثلاثُ خطواتٍ من ستّ (التشخيصُ والإصلاحُ والاختبار ✓؛ الدمجُ والنشرُ ومعاينةُ المالك ✗) — اشتقاقٌ لا قياس؛ لا يُغلق قبل نشره "
        "ومعاينتِه.** **خارجَ النطاق ولم يُعالَج:** واجهةُ الجرس المنسدلة تقصّ النصَّ عند 100 حرف (ولا يستعملها القالبُ اليوم)، وسحبُ الإشعار لليسار على اللمس يحدّده "
        "مقروءاً دون تأكيد. لا مؤشّرَ في الخارطة يقيسه؛ يحرسه اختبارٌ ساكن. **قاعدةُ الإدراج:** لم يكن للعمل بندٌ (ملاحظةٌ ← ابنٌ ← عائلةٌ ← N-…؛ وM-02 يخصّ مساحةَ اللمس لا نصَّ "
        "الإشعار) فعُيِّن N-045 في مسار الواجهة بطلب المالك.",
        736,
    ),
]

# ملاحظاتٌ على بنودٍ قائمة تُلحق مرّةً واحدة (بلا لمس الحالة ولا التقدّم)
NOTES = [
    (
        "REP-07",
        "**REP-07b (#623) اندمج 19:45 ونُشر ضمن main@4e27672 نحو 19:58 (تغييرٌ غيرُ تشغيليّ):** الأسماءُ الكاملةُ للموظّفين خارج الشيفرة — الجدولُ وقواعدُ الربط "
        "تُقرأ من ملفّين خاصّين خارج المستودع. **المقيس (8097، على main@4e276726 بعد الدمج):** مسحٌ محلّيٌّ بقائمةٍ خاصّةٍ موسَّعةٍ على الملفّات العربيّة المتتبَّعة: "
        "لا اسمَ كاملاً باقياً (كان في عدّة ملفّات)؛ وما بقي مقصودٌ (اسمُ المالك في وثائقه — استثناءٌ أكّده). **لا يُعلن إغلاقُه قبل خضرة CI على main** (8097). "
        "وهو خارجَ معيار REP-07.",
    ),
]


def _add_pr(existing, token):
    """تُضيف رمزَ الطلب دون تكرارٍ ودون تجاوز حدّ الحقل 64 (وإلّا يبقى الحقلُ كما هو)."""
    if not token:
        return existing
    joined = " ".join(dict.fromkeys([*existing.split(), *token.split()]))
    return joined if len(joined) <= 64 else existing


def sync(item_model):
    """تُطبِّق تحديثاتِ البنود الحارسة؛ تُرجع رموزَ ما تغيّر."""
    changed = []
    for code, expected, status, progress, clear_gate, pr, line in UPDATES:
        item = item_model.objects.filter(code=code).first()
        if item is None or (item.status, item.progress) != expected or line in item.note:
            continue
        item.status, item.progress = status, progress
        item.pr = _add_pr(item.pr, pr)
        if clear_gate:
            item.gate = ""
        item.note = f"{item.note}\n{STAMP} {line}".strip()
        item.save(update_fields=["status", "progress", "pr", "gate", "note", "updated_at"])
        changed.append(code)
    return changed


def add_new_items(item_model):
    """تُنشئ البنودَ الغائبة؛ لا تُعيد كتابةَ بندٍ موجود؛ تُرجع رموزَ ما أُنشئ."""
    created = []
    for (
        code,
        src,
        lane,
        title,
        deps,
        criterion,
        status,
        progress,
        start,
        end,
        basis,
        effort,
        gate,
        pr,
        ref,
        note,
        order,
    ) in NEW_ITEMS:
        if item_model.objects.filter(code=code).exists():
            continue
        item_model.objects.create(
            code=code,
            src=src,
            lane=lane,
            title=title,
            status=status,
            progress=progress,
            start_date=start,
            end_date=end,
            date_basis=basis,
            effort=effort,
            deps=deps,
            criterion=criterion,
            gate=gate,
            pr=pr,
            ref=ref,
            note=f"{STAMP} {note}",
            sort_order=order,
        )
        created.append(code)
    return created


def sync_notes(item_model):
    """تُلحق الملاحظاتِ بالبنود القائمة مرّةً واحدة؛ تُرجع رموزَ ما تغيّر."""
    changed = []
    for code, line in NOTES:
        item = item_model.objects.filter(code=code).first()
        if item is None or line in item.note:
            continue
        item.note = f"{item.note}\n{STAMP} {line}".strip()
        item.save(update_fields=["note", "updated_at"])
        changed.append(code)
    return changed


def forwards(apps, schema_editor):
    item_model = apps.get_model("roadmap", "RoadmapItem")
    # قاعدةٌ بلا استيراد (اختبار، شجرةٌ جديدة): لا شيء يُزامَن، ولا بنودٌ يتيمة.
    if not item_model.objects.exists():
        return
    sync(item_model)
    add_new_items(item_model)
    sync_notes(item_model)


class Migration(migrations.Migration):
    dependencies = [
        ("roadmap", "0032_sync_items_2026_09_25j"),
    ]

    operations = [
        migrations.RunPython(forwards, migrations.RunPython.noop),
    ]
