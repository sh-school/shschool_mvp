"""الخارطةُ الثامنةُ والعشرون (2026-09-26 مساءً): SCH-08 يُغلق بقياسٍ قرائيٍّ شغّله المالكُ بيده على الإنتاج.

القياسُ النهائيّ المجدوَل صباحَ الأحد 06:47 لن يعمل (الجلسةُ المنفِّذةُ غيرُ متّصلة ولا مهمّةَ له في سجلّ المهامّ المجدولة)، فشغّل المالكُ سكربتَ القياس القرائيّ بيده الليلةَ على الإنتاج.
والقرارُ لأمين الخارطة (لا للمايسترو): القياسُ يغطّي الأيّامَ الخمسةَ كلَّها بما فيها الأحد، فيكفي لإغلاق البند بمعياره.

حارسةٌ كسابقاتها: لا يُلمس بندٌ إلّا إن طابقت حالتُه وتقدّمُه آخرَ لقطة، ولا تُكرَّر ملاحظةٌ.
والمستودعُ عامّ: نصوصٌ محايدة. **لا يُغلق بندٌ بلا قياس.**
"""

from django.db import migrations

STAMP = "[2026-09-26]"

# (الرمز، (الحالة، التقدّم) المتوقَّعان، الحالة الجديدة، التقدّم الجديد، سطر الملاحظة)
UPDATES = [
    (
        "SCH-08",
        ("doing", 95),
        "done",
        100,
        "**SCH-08 منجز بقياسٍ قرائيٍّ شغّله المالكُ بيده على الإنتاج (2026-09-26 ~18:30 الدوحة، سكربتُ `verify_sessions.py` عبر أداة التشغيل، قراءةٌ فقط؛ نقلاً عن «مايسترو إدارة الجلسات»):** "
        "التوليدُ المعتمَد 15007906 (2026-2027، 869 حصّة) وكلُّ الحصص الحيّة الـ869 منه؛ جلساتُ 27/9 ← 1/10 (خمسةُ أيّام: 176 و177 و175 و176 و165) كلُّها `scheduled`، لا ناقصَ ولا زائدَ ولا فترةَ بلا رقم؛ "
        "والفرقُ الوحيد جلستان (الأحد والثلاثاء) وكلٌّ منهما تبديلُ معلّمٍ مسجَّلٌ (swapped=1) من جلسة التشغيل — متوقَّعٌ لا خلل. **قرارُ الإغلاق لأمين الخارطة:** القياسُ يغطّي الأيّامَ الخمسةَ كلَّها بما فيها الأحد فيحلّ محلّ القياس النهائيّ "
        "المجدوَل 06:47 صباحَ الأحد (لن يعمل: الجلسةُ المنفِّذةُ غيرُ متّصلةٍ ولا مهمّةَ له في سجلّ المهامّ المجدولة). **تحفّظ:** قد يعيد المالكُ التشغيلَ صباحاً قبل 07:00 اختياريّاً؛ فإن اختلف الرقمُ فتُعاد الملاحظة. "
        "**تنبيهٌ للخارطة:** بنودٌ أُسندت لتلك الجلسة (SCH-11 وOWN-31 وأرقامُ SK1/SK4 لقرار D-25 بحلول 10-04) تحتاج منفِّذاً حيّاً.",
    ),
]


def sync(item_model):
    """تُطبِّق تحديثاتِ البنود الحارسة؛ تُرجع رموزَ ما تغيّر."""
    changed = []
    for code, expected, status, progress, line in UPDATES:
        item = item_model.objects.filter(code=code).first()
        if item is None or (item.status, item.progress) != expected or line in item.note:
            continue
        item.status, item.progress = status, progress
        item.note = f"{item.note}\n{STAMP} {line}".strip()
        item.save(update_fields=["status", "progress", "note", "updated_at"])
        changed.append(code)
    return changed


def forwards(apps, schema_editor):
    item_model = apps.get_model("roadmap", "RoadmapItem")
    # قاعدةٌ بلا استيراد (اختبار، شجرةٌ جديدة): لا شيء يُزامَن، ولا بنودٌ يتيمة.
    if not item_model.objects.exists():
        return
    sync(item_model)


class Migration(migrations.Migration):
    dependencies = [
        ("roadmap", "0040_sync_items_2026_09_26d"),
    ]

    operations = [
        migrations.RunPython(forwards, migrations.RunPython.noop),
    ]
