"""مزامنةُ الخارطة الحاديةَ والعشرون (2026-09-25 مساءً): ما نُشر على main@86335dd (الجمعة 21:15 الدوحة، بشهادة جلسة النشر).

حارسةٌ كسابقاتها: لا يُلمس بندٌ إلّا إن كانت حالتُه وتقدّمُه ما في آخر لقطة، ولا تُكرَّر ملاحظةٌ، ورمزُ الطلب يُضاف بحدّ الحقل 64، ويُنشأ الغائبُ وحدَه.
والمستودعُ عامّ: نصوصٌ محايدة. **لا يُغلق بندٌ بلا قياس:** ما لم يُتحقَّق يُذكر في ملاحظته وتقدّمُه اشتقاقٌ لا قياس.

- **N-045 → 83%** (#628 — إصلاح نصّ الإشعار): نُشر؛ تبقى معاينةُ المالك.
- **REP-17 → قيد التنفيذ 50%** (#636): الشطرُ (أ) منجزٌ ومنشور؛ (ب) بانتظار U-34 وقرار المالك.
- **N-046 جديد** (#634): خادمُ معاينةٍ مقيمٌ واحد يتبع main بدل خادمٍ لكلّ جلسة.
- **N-047 جديد** (#632): ورقةُ الجدول العامّ — الترويسةُ للورق فقط وسطرُ «الوزارة | العام | الأسبوع» تحت الجدول؛ منشورٌ ومتحقَّقٌ من التصيير، وتبقى رؤيةُ المالك.
"""

import datetime

from django.db import migrations

STAMP = "[2026-09-25]"
D = datetime.date


# (الرمز، (الحالة، التقدّم) المتوقَّعان، الحالة الجديدة، التقدّم الجديد، رفعُ البوّابة، رمزُ الطلب يُضاف، سطر الملاحظة)
UPDATES = [
    (
        "N-045",
        ("doing", 50),
        "doing",
        83,
        False,
        "",
        "**#628 اندمج (fe9a42fa) ونُشر ضمن main@86335dd (الجمعة 21:15 الدوحة) وتحقّقت منه جلسةُ النشر:** نصُّ الإشعار كاملاً في الصندوق بلا قصٍّ بالحرف ولا "
        "بالأسطر. **لم يُتحقَّق:** معاينةُ المالك لصندوق الإشعارات على الحاسوب وعلى جهاز لمسٍ حقيقيّ. **التقدّم 83% = خمسُ خطواتٍ من ستّ (التشخيصُ والإصلاحُ "
        "والاختبارُ والدمجُ والنشرُ ✓؛ معاينةُ المالك ✗) — اشتقاقٌ لا قياس؛ ولا يُغلق قبل معاينته.**",
    ),
    (
        "REP-17",
        ("todo", 0),
        "doing",
        50,
        False,
        "#636",
        "**الشطرُ (أ) منجزٌ ومنشور (#636، إيداعُ الدمج 86335dd4، ضمن main@86335dd الجمعة 21:15 الدوحة، وتحقّقت منه جلسةُ النشر):** (1) عنوانا مراقبَي GitHub "
        "المجدولَين (monitor.yml وworker-heartbeat.yml) صارا «فحصٌ ثانويّ… بأفضل جهد» بدل وعدٍ بوتيرة (قيست 7% و4.4% من وتيرتيهما يوم 09-25) مع تعليقٍ ووثيقةٍ "
        "تقولان إنّ غيابَ القضايا لا يثبت سلامةَ الإنتاج وإنّ المراقبَ الأوّل Sentry Uptime؛ (2) كنارٌ تلقائيّ (post-deploy-canary.yml) عند deployment_status "
        "ناجحٍ لبيئة Railway `shschool_mvp / production` يتحقّق أنّ الـcommit المنشور صار حيّاً وأنّ صفحةَ الدخول تُرسم، ويفتح قضيّةَ «DEPLOY FAILED» إن فشل "
        "ويغلقها نجاحٌ لاحق ولا يدور في حلقةٍ مع خطّ النشر اليدويّ؛ (3) حارسان test_monitoring_claims_are_honest وtest_canary_check_script (16 اختباراً، "
        "وأربعُ طفراتٍ أُسقطت). **المقيس:** الكنارُ نجح أوّلَ تشغيلٍ تلقائيّاً بعد النشر (جلسةُ النشر). **يبقى:** الشطرُ (ب) «نطاقٌ مخصَّص + مقعد + مراقبُ Cron» "
        "بانتظار U-34 وقرار المالك — فلا يُغلق البند. RK8 لا يتغيّر (361 دقيقة، قياسُ أداة GitHub). **التقدّم 50% = الشطرُ (أ) من شطرَين — اقتراحُ صاحبه "
        "(أمين المستودع) لا قياس.** ومواعيدُ البند لم تُعدَّل.",
    ),
]

# بنودٌ جديدة: (الرمز، المصدر، المسار، العنوان، الاعتماديّات، المعيار، الحالة، التقدّم، البداية، النهاية، أساسُ التاريخ، الجهد، البوّابة، طلب الدمج، المرجع، الملاحظة، الترتيب)
NEW_ITEMS = [
    (
        "N-046",
        "NEW",
        "ops",
        "خادمُ معاينةٍ مقيمٌ واحد يتبع main بدل خادمٍ لكلّ جلسة (المعاينةُ المركزيّة)",
        "",
        "خادمٌ واحدٌ مقيمٌ يتبع main بوضع الإنتاج وبياناتٌ مرجعيّةٌ غيرُ شخصيّةٍ عند الطلب؛ تثبيتٌ مؤقّتٌ لشجرة جلسةٍ لعملٍ لم يصر طلبَ دمج؛ وذاكرةُ حاويات "
        "التطوير تنخفض؛ وقاعدةُ «الخادمُ المقيمُ الوحيد» تُكتب في CLAUDE.md بإذن المالك.",
        "doing",
        50,
        D(2026, 9, 25),
        None,
        "قرارُ المالك 2026-09-25 (نقلاً عن جلسة Docker)",
        1.0,
        "",
        "#634",
        "Docker والسيرفرات المحلية",
        "**#634 اندمج (95097715) ودخل main@86335dd (الجمعة 21:15 الدوحة؛ بلا هجرةٍ وبلا أثرٍ على الإنتاج).** القرارُ (المالكُ لجلسة Docker والسيرفرات المحلية "
        "2026-09-25: «سيرفر واحد يعمل فقط ونوفّر الذاكرة» — **نقلٌ لم يؤكّده المالكُ لجلسة الخارطة مباشرةً**): (1) يتبع main لا رأسَ الإنتاج وحدَه؛ (2) بياناتُه نسخةٌ "
        "من القاعدة المزروعة وبياناتٌ مرجعيّةٌ غيرُ شخصيّةٍ عند الطلب — **لا بياناتٍ شخصيّةً من الإنتاج إلى الجهاز**؛ (3) يُثبَّت مؤقّتاً على شجرة جلسةٍ لعملٍ لم يصر "
        "طلبَ دمج. **المقيس (2026-09-25):** ذاكرةُ الحاويات 2,313 ← 899 ميغا (−61%) بـ`docker stats --no-stream` قبل إيقاف خادم الجذر وإطفاء الجلسات لخوادمها وبعده؛ "
        "المعاينةُ نفسُها خادمٌ 133–175 وعاملٌ 146 ميغا؛ والتزامُ الجهاز 89% ← 86% بعد إفراغ ذاكرة آلة WSL المخبَّأة (يتراجع جزئيّاً مع الوقت)؛ والفحصُ الحيّ: /health/ "
        "سليمٌ والـcommit مطابق وCSP مفروضة. **ما بقي:** الإقلاعةُ الثانية على main، والبياناتُ المرجعيّةُ، وقاعدةُ CLAUDE.md بإذنٍ بنصّه، وتشغيلُ watch دائماً "
        "(مهمّةٌ مجدولةٌ بإذن المالك)، وبنودُ ذاكرةٍ عند المالك (تعطيلُ Kubernetes وإضافاتِ Docker Desktop وحذفُ MCP_DOCKER). المرجع: docs/deployment/local_preview.md. "
        "**التقدّم 50% = ثلاثُ خطواتٍ من ستّ (الكودُ والدمجُ والتحقّقُ الحيّ ✓؛ الإقلاعةُ الثانيةُ والبياناتُ والقاعدةُ ✗) — اشتقاقٌ لا قياس.**",
        737,
    ),
    (
        "N-047",
        "NEW",
        "backend",
        "ورقةُ الجدول العامّ: الترويسةُ للورق فقط على شاشة المنصّة، وسطرُ «الوزارة | العام | الأسبوع» تحت الجدول",
        "",
        "شاشةُ المنصّة بلا ترويسةٍ فوق الجدول والورقُ/PDF بها؛ والسطرُ تحت الجدول قبل الذيل؛ وصفحةٌ واحدةٌ على A3؛ وما على الشاشة يطابق ما يُطبع "
        "(D-16 المادّة 7.7)؛ وحارسٌ يثبّت ذلك؛ ورؤيةُ المالك للأثر المرئيّ بعد النشر.",
        "doing",
        50,
        D(2026, 9, 25),
        None,
        "بلاغُ المالك 2026-09-25",
        1.0,
        "",
        "#632",
        "جدول · التشغيل",
        "**#632 اندمج 20:48 الدوحة (041f1d46) ونُشر ضمن main@86335dd (الجمعة 21:15) وتحقّقت منه جلسةُ النشر:** /health/ ok والدخول 200 وصفرُ أخطاء، وتصييرُ "
        "print_schedule.html في عامل Celery سليمٌ بوضعَي for_pdf (True بلا سكربت actions، وFalse بلا استثناء)، وخطّا Amiri حاضران في الويب والعامل. قالبٌ فقط بلا "
        "CSS في static/css/custom ولا هجرة ولا beat، فميزانيّةُ CSS الخام لم تتغيّر. **المقيس (جدول · التشغيل، 2026-09-25):** (1) متصفّحٌ حقيقيّ على لقطةٍ مرسومة: "
        "بوضع الإطار (embed=1) الترويسةُ مخفيّةٌ والسطرُ بعد الجدول، والورقةُ المستقلّةُ بها؛ (2) طباعةُ A3 (PDF بـWeasyPrint وراجعتْ صورتَه): الترويسةُ بالأعلى "
        "والسطرُ تحت الجدول قبل الذيل، صفحةٌ واحدة، في ثلاث حالات (أسبوعٌ بتبديلٍ وتعويض، وبثلاث ملاحظات، وأسبوعُ إجازة)؛ (3) اختباراتٌ: ثلاثةٌ جديدةٌ في "
        "tests/test_week_paper.py + 55 نجاحاً + 43 من حرّاس الهويّة. **لم يُقَس:** رؤيةُ المالك (شاشةُ المنصّة وطباعةُ PDF فعليّة). **مفتوح:** (أ) طلبُ رؤية "
        "الوزارة في السطر (#641) عند جلسة النشر ولم يُدمج بعد؛ (ب) الرؤيةُ ستظهر مرّتين على الورق (السطرُ والذيل) حتى يقرّر المالك؛ (ج) الشريطُ الواحد لصفحة "
        "الجدول بندٌ مقترَحٌ آخر (LAY-09) لا هذا. **التقدّم 50% = خطوتان من أربع (نشرُ #632 والقياسُ ✓؛ دمجُ #641 ومعاينةُ المالك ✗) — اشتقاقٌ لا قياس؛ ولا يُغلق "
        "قبل معاينة المالك.** لم يكن للعمل بندٌ فعُيِّن N-047 (قاعدةُ الإدراج: ملاحظةٌ ← ابنٌ ← عائلةٌ ← N-…؛ وN-041 مغلقٌ بمعيارٍ آخر).",
        738,
    ),
]


def _add_pr(existing, token):
    """تُضيف رمزَ الطلب دون تكرارٍ ودون تجاوز حدّ الحقل 64 (وإلّا يبقى الحقلُ كما هو)."""
    if not token:
        return existing
    joined = " ".join(dict.fromkeys([*existing.split(), *token.split()]))
    return joined if len(joined) <= 64 else existing


def sync(item_model):
    """تُطبِّق تحديثاتِ البنود الحارسة؛ تُرجع رموزَ ما تغيّر."""
    changed = []
    for code, expected, status, progress, clear_gate, pr, line in UPDATES:
        item = item_model.objects.filter(code=code).first()
        if item is None or (item.status, item.progress) != expected or line in item.note:
            continue
        item.status, item.progress = status, progress
        item.pr = _add_pr(item.pr, pr)
        if clear_gate:
            item.gate = ""
        item.note = f"{item.note}\n{STAMP} {line}".strip()
        item.save(update_fields=["status", "progress", "pr", "gate", "note", "updated_at"])
        changed.append(code)
    return changed


def add_new_items(item_model):
    """تُنشئ البنودَ الغائبة؛ لا تُعيد كتابةَ بندٍ موجود؛ تُرجع رموزَ ما أُنشئ."""
    created = []
    for (
        code,
        src,
        lane,
        title,
        deps,
        criterion,
        status,
        progress,
        start,
        end,
        basis,
        effort,
        gate,
        pr,
        ref,
        note,
        order,
    ) in NEW_ITEMS:
        if item_model.objects.filter(code=code).exists():
            continue
        item_model.objects.create(
            code=code,
            src=src,
            lane=lane,
            title=title,
            status=status,
            progress=progress,
            start_date=start,
            end_date=end,
            date_basis=basis,
            effort=effort,
            deps=deps,
            criterion=criterion,
            gate=gate,
            pr=pr,
            ref=ref,
            note=f"{STAMP} {note}",
            sort_order=order,
        )
        created.append(code)
    return created


def forwards(apps, schema_editor):
    item_model = apps.get_model("roadmap", "RoadmapItem")
    # قاعدةٌ بلا استيراد (اختبار، شجرةٌ جديدة): لا شيء يُزامَن، ولا بنودٌ يتيمة.
    if not item_model.objects.exists():
        return
    sync(item_model)
    add_new_items(item_model)


class Migration(migrations.Migration):
    dependencies = [
        ("roadmap", "0033_sync_items_2026_09_25k"),
    ]

    operations = [
        migrations.RunPython(forwards, migrations.RunPython.noop),
    ]
