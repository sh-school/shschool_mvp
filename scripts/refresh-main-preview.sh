#!/usr/bin/env bash
# ══════════════════════════════════════════════════════════════
#  تحديثُ معاينة main المحليّة — مُستبدَلٌ بـ scripts/preview.sh
# ══════════════════════════════════════════════════════════════
#  كانت المعاينةُ (2026-09-18) تُحدَّث بهذا السكربت يدويّاً بعد كلّ دمج، وبوضع التطوير. صارت
#  تتبع main وحدَها وبوضع الإنتاج: انظر scripts/preview.sh (`watch` يتبع، و`status` يقارن بالإنتاج).
#  يبقى هذا الاسمُ لمن اعتاده: يُطبّق فوراً ما وصل main دون انتظار السكون.
# ══════════════════════════════════════════════════════════════
exec bash "$(dirname "${BASH_SOURCE[0]}")/preview.sh" sync --now
